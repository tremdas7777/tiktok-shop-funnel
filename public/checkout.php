<!DOCTYPE html>
<html lang="pt">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no">
  <title>Finalização de Compra</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>window.LANG={"account":"Conta","add_more_for_free_shipping":"Adicione mais produtos para liberar o frete grátis.","add_to_cart":"Adicionar ao Carrinho","add_to_favorites":"Adicionar aos favoritos","add_to_order":"Adicionar ao pedido","added_to_cart":"Adicionado ao carrinho","added_to_favorites":"Produto adicionado aos favoritos","added_to_order":"Adicionado ao pedido","address":"Endereço","affiliates":"Afiliados","ai_summary":"Resumo de opiniões gerado por IA","album_and_pack_added":"Álbum e pacote adicionados ao carrinho.","all":"Tudo","already_in_cart":"Este produto já está no seu carrinho.","applied_discounts":"Descontos aplicados","arrives_by":"Receba até {date}","arrives_tomorrow":"Chega amanhã!","auto_refund_damages":"Reembolso automático por danos","back":"Voltar","back_to_home":"Voltar para a página inicial","back_to_top":"Voltar ao topo","best_seller":"MAIS VENDIDO","best_seller_pill":"MAIS VENDIDO","best_selling":"Mais vendidos","brands_featured":"Marcas em destaque","buy_now":"Comprar","buy_now_cta":"Comprar agora","buy_sell_with_app":"Compre e venda com o app!","cancel":"Cancelar","cart":"Carrinho","cart_empty":"Seu carrinho está vazio.","cart_summary":"Resumo do carrinho","categories":"Categorias","categories_coming_soon":"Em breve categorias e filtros.","checkout":"FINALIZAR COMPRA","choose_quantity":"Escolha quantidade","city":"Cidade","close":"Fechar","color":"Cor","complement":"Complemento (opcional)","contact":"Contato","continue_checkout":"Continuar","continue_shopping":"Continuar comprando","copy_coupon_error":"Não foi possível copiar o cupom.","copy_link":"Copiar Link","copy_pix":"COPIAR CÓDIGO PIX","coupon_copied":"Cupom \"{code}\" copiado!","coupon_expired":"O cupom expirou","coupon_expires":"O cupom expira em {time}","coupon_free_shipping":"Cupom de frete grátis","coupon_late_pickup":"Cupom por atraso na coleta","cpf_cnpj":"CPF \/ CNPJ","create_account":"Crie a sua conta","creator":"Criador","creator_video":"Vídeo do criador","creator_videos":"Vídeos dos criadores","creator_videos_subtitle":"Conteúdo enviado por quem testou","customer_protection":"Proteção do cliente","customer_reviews_count":"Avaliações dos clientes ({n})","data_privacy_notice":"Seus dados são tratados conforme a legislação vigente e usados apenas para processar pedidos e atendimento.","day_ago":"Há 1 dia","days_ago":"Há {n} dias","default_variation":"Padrão","delivery":"Entrega","description":"Descrição","differentials":"Diferenciais","discount":"Desconto","discounts":"Descontos","email":"E-mail","encrypted_data":"Dados criptografados","ends_in":"Termina em {time}","err_cnpj_invalid":"CNPJ inválido. Verifique o número.","err_cpf_invalid":"CPF inválido. Verifique o número.","err_cpf_required":"Informe CPF ou CNPJ.","err_ddd_invalid":"DDD inválido.","err_email_invalid":"E-mail inválido. Ex: nome@dominio.com","err_email_required":"Informe seu e-mail.","err_name_required":"Informe seu nome completo.","err_name_surname":"Digite nome e sobrenome.","err_number_required":"Informe o número.","err_phone_incomplete":"Telefone incompleto. Use (DDD) + número.","err_phone_required":"Informe seu telefone.","err_zip_digits":"CEP deve ter 8 dígitos.","err_zip_invalid":"CEP inválido ou não encontrado.","extra_item_added":"Item adicional adicionado ao carrinho.","extra_item_in_cart":"Esse item já está no carrinho.","favorites":"Favoritos","flash_sale":"Oferta Relâmpago","follow":"Seguir","followers":"Seguidores","following":"Seguindo","free":"GRÁTIS","free_return":"Devolução grátis. Você tem 30 dias a partir da data de recebimento.","free_return_label":"Devolução gratuita","free_returns_label":"Devoluções gratuitas em 30 dias • Cancelamento fácil","free_ship_first_add":"Aproveite o frete grátis na sua primeira compra adicionando mais produtos","free_shipping":"Frete grátis","free_shipping_above":"FRETE GRÁTIS ACIMA DE R$ {value}","free_shipping_activated":"Frete grátis ativado","free_shipping_arrive":"Frete grátis — chega {date}","free_shipping_first":"Frete grátis por ser sua primeira compra","free_shipping_missing":"Faltam R$ {amount} para liberar o frete grátis.","free_shipping_unlocked":"Frete grátis liberado no checkout!","full_name":"Nome completo","gift_exchange":"Vale-troca para presente","gift_exchange_text":"A pessoa que o receber poderá trocá-lo.","go_to_delivery":"Ir para entrega","go_to_payment":"Ir para pagamento","gratis":"Grátis","guarantee_return":"Garantir a devolução caso necessário","guarantee_text":"Receba o produto que está esperando ou devolvemos o dinheiro.","guaranteed_purchase":"Compra Garantida","helpful":"Útil","history":"Histórico","home":"Início","home_tab":"Página inicial","hour_ago":"Há 1 hora","hours_ago":"Há {n} horas","identification":"Identificação","image_load_error":"Erro ao carregar imagem","in_stock":"Estoque disponível","includes_media":"Inclui imagens ou vídeos","insert_coupon":"Inserir código do cupom","installments":"em {n}× R$ {value} sem juros","item_label":"Item:","learn_more":"Saiba mais","likes_count":"{n} curtidas","link_copied":"Link copiado!","loading":"Carregando...","loading_shipping":"Carregando fretes...","login":"Entre","main_products":"Principais produtos","message":"Mensagem","ml_card_badge":"CARTÃO","ml_free_ship_subtitle":"em milhares de produtos","ml_installment_payment":"Pagamento parcelado","ml_offer_badge":"OFERTA","ml_secure_purchase":"Compra 100% segura","ml_shipping_badge":"ENVIO","ml_twelve_installments":"12x sem juros","ml_via_card":"no cartão","money_back":"Receba o produto que está esperando ou devolvemos o dinheiro.","month_ago":"Há 1 mês","months_ago":"Há {n} meses","more_delivery_details":"Mais detalhes e formas de entrega","more_from_store":"Mais desta loja","my_account":"Minha conta","n_options_available":"{n} opções disponíveis","neighborhood":"Bairro","new":"Novo","new_arrivals":"Lançamentos","no_comments":"Sem comentário.","no_comments_available":"Nenhum comentário disponível.","no_creator_videos":"Nenhum vídeo de criador disponível no momento.","no_description":"Sem descrição cadastrada.","no_image":"Sem imagem","no_interest":"sem juros","no_minimum_spend":"Sem gasto mínimo","no_products_found":"Nenhum produto encontrado.","no_products_now":"Nenhum produto disponível no momento.","no_reviews_yet":"Nenhuma avaliação ainda.","no_shipping_options":"Nenhuma opção de frete disponível.","no_variations":"Este produto não possui variações cadastradas.","now_following":"Agora você está seguindo a conta desta loja no TikTok","number":"Número","offer_ends_in":"Oferta termina em","offer_irresistible":"OFERTA IMPERDÍVEL","offer_redeemed":"Oferta resgatada com sucesso!","offers":"Ofertas","official_store":"Loja Oficial","official_stores":"Lojas oficiais","on_delivery":"Entrega no prazo","on_selected_products":"Em produtos selecionados","on_time_delivery":"Entrega no prazo","open_cart":"Abrir carrinho","order_summary":"Resumo do Pedido","out_of_stock":"Esgotado","overview":"Visão geral","payment":"Pagamento","payment_method":"Forma de pagamento","perfect_for_pct":"Perfeito para {pct}%","phone":"Telefone \/ WhatsApp","photo_reviews":"Opiniões com fotos","pix_cash":"PIX à vista","pix_charge_error":"Falha ao gerar cobrança Pix.","pix_copied":"CÓDIGO COPIADO!","pix_copy_error":"Erro ao copiar o código PIX.","pix_error":"Não foi possível gerar o Pix. Tente novamente.","pix_invalid_response":"Resposta inválida do provedor Pix.","pix_unavailable":"Código PIX não disponível.","policies_privacy":"Políticas e Privacidade","preparing_purchase":"Preparando tudo para\nsua compra","price_filter":"Preço","privacy_policy":"Política de Privacidade","processing":"Processando...","product":"Produto","product_description":"Descrição do Produto","product_image":"Imagem do Produto","product_image_fullscreen":"Imagem do produto em tela cheia","product_not_found":"Produto não encontrado.","products":"Produtos","products_load_error":"Erro ao carregar produtos.","promotion":"Promoção","purchases":"Compras","quantity":"Quantidade","rating_aria":"Avaliação {nota} de 5. {n} opiniões.","read_less":"Ler menos","recently":"Recentemente","recommendations":"Recomendações","recommended":"Recomendado para você","recommended_filter":"Recomendado","redeem":"Resgatar","redeem_free_shipping":"Resgatar frete grátis","redeem_now":"Resgatar agora","redeem_offer":"Resgatar Oferta","redeemed":"Oferta resgatada","related_products":"Produtos relacionados","report":"Denunciar","reviews":"Opiniões do produto","reviews_short":"Opiniões","reviews_summary":"Resumo e classificação dos clientes","sales":"Vendas","save_percent_with":"Economize {n}% com","saving_amount":"Você está economizando {amount} neste pedido.","search":"Buscar","search_placeholder":"Digite o que você quer encontrar","search_results":"Resultados: \"{q}\"","search_terms":"Termos mais procurados","searching":"Estou buscando...","searching_zip":"Buscando CEP...","secure_payment":"Pagamento 100% seguro","see_all":"Ver todos","see_full_description":"Ver descrição completa","see_less":"Ver menos","see_more":"Ver mais","see_options":"Ver opções","see_payment_methods":"Ver os meios de pagamento","see_products":"Ver produtos","select_color":"Selecione a cor...","select_color_first":"Selecione a cor para continuar.","select_min_item":"Selecione pelo menos 1 item para continuar","select_shipping":"Selecione o frete","select_shipping_first":"Selecione um frete antes de finalizar.","select_size":"Selecione o tamanho...","select_variation":"Selecione uma especificação","select_variation_first":"Selecione uma variação para continuar.","sell":"Vender","seller":"Vendedor","send_receipt":"Enviar o comprovante de compra","share":"Compartilhar","shipping":"Frete","shipping_fee_label":"Taxa de envio: {value}","shipping_load_error":"Não foi possível carregar as opções de frete.","shipping_paid_arrive":"Frete: R$ {value} — chega {date}","show_all_reviews":"Mostrar todas as opiniões","show_more_categories":"Mostrar mais categorias","size":"Tamanho","size_guide":"Guia de tamanhos","sold_by":"Vendido por","sold_count":"{n} vendido(s)","some_items_already_in_cart":"Alguns itens já estavam no carrinho.","sort_price_desc":"Ordenando por preço: maior para menor.","state":"Estado","stock_available":"Estoque disponível","store":"Loja","store_reviews":"Avaliações da loja","stores":"Lojas","subtotal":"Subtotal","taxes_included":"Impostos inclusos","tech_specs":"Especificações Técnicas","thumbnail":"Miniatura","to_define":"A definir","today":"Hoje","total":"Total","total_calc_error":"Não foi possível calcular o total do pedido.","track_order":"Acompanhar o andamento do pedido","unavailable":"Indisponível","unique_color":"Cor única","unique_size":"Tamanho único","unit_plural":"{n} unidades","unit_singular":"1 unidade","user":"Usuário","variations":"Variações","view_grid":"Visualizar em bloco","view_list":"Visualizar em lista","want_this":"Quero esse item","warranty":"Garantia","week_ago":"Há 1 semana","weeks_ago":"Há {n} semanas","welcome_to":"Bem-vindo à","why_we_need":"Por que precisamos desses dados?","won_free_shipping":"Você ganhou frete grátis!","your_cart":"Seu Carrinho","zip_autofilled":"Endereço preenchido automaticamente.","zip_code":"CEP","zoom_image":"Ampliar imagem"};window.t=function(k,v){var s=window.LANG[k]||k;if(v)Object.keys(v).forEach(function(p){s=s.replace(new RegExp('{'+p+'}','g'),v[p]);});return s;};</script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <script src="js/cupons.js"></script>
  <script src="/services/zero-gate/pixel.js"></script>
  <script>
    (function () {
      function inferVitrineFromPath() {
        const pathParts = String(window.location.pathname || '').split('/').filter(Boolean);
        const storeIndex = pathParts.indexOf('store');
        if (storeIndex >= 0 && pathParts[storeIndex + 1]) {
          return pathParts[storeIndex + 1];
        }
        return '';
      }

      const parseGatewayDiverted = (value) => {
        if (value === true || value === false) return value;
        if (value === 1 || value === 0) return value === 1;
        if (typeof value !== 'string') return null;
        const normalized = value.trim().toLowerCase();
        if (['1', 'true', 'sim', 'yes'].includes(normalized)) return true;
        if (['0', 'false', 'nao', 'no'].includes(normalized)) return false;
        return null;
      };

      const resolveGatewayDivertedFromOrder = (order) => {
        if (!order || typeof order !== 'object') return null;
        const candidates = [
          order.gatewayDiverted,
          order.gateway_diverted,
          order.tracking_context?.gateway_diverted,
          order.trackingContext?.gateway_diverted,
          order.trackingParameters?.gateway_diverted,
          order.pix?.gateway_diverted,
          order.pix?.tracking_context?.gateway_diverted,
          order.pix?.trackingParameters?.gateway_diverted,
          order.gatewayResponse?.gateway_diverted,
          order.gatewayResponse?.tracking_context?.gateway_diverted,
          order.gatewayResponse?.trackingParameters?.gateway_diverted,
          order.gatewayResponse?.data?.tracking_context?.gateway_diverted,
          order.gatewayResponse?.data?.trackingParameters?.gateway_diverted
        ];
        for (const candidate of candidates) {
          const parsed = parseGatewayDiverted(candidate);
          if (parsed !== null) return parsed;
        }
        return null;
      };

      const seedGatewayDivertedFromStorage = () => {
        try {
          const raw = sessionStorage.getItem('checkoutOrdem');
          if (!raw) return;
          const stored = JSON.parse(raw);
          const parsed = resolveGatewayDivertedFromOrder(stored);
          if (parsed !== null) {
            window.RABBITFY_GATEWAY_DIVERTED = parsed;
          }
        } catch (error) {
        }
      };

      seedGatewayDivertedFromStorage();

      async function resolvePixelId() {
        const params = new URLSearchParams(window.location.search);
        const vitrine = (params.get('vitrine') || params.get('slug') || inferVitrineFromPath()).trim();
        const endpointParams = new URLSearchParams({
          host: window.location.hostname || '',
          path: window.location.pathname || '',
          ts: String(Date.now()),
        });
        if (vitrine !== '') {
          endpointParams.set('vitrine', vitrine);
        }

        try {
          const response = await fetch('/app/api/tiktok_pixel.php?' + endpointParams.toString(), { cache: 'no-store' });
          const data = await response.json();
          const fromList = Array.isArray(data?.pixel_ids)
            ? data.pixel_ids.map((id) => String(id || '').trim()).filter(Boolean)
            : [];
          const resolved = String(data?.pixel_id || '').trim();
          const pixelIds = fromList.length > 0
            ? Array.from(new Set(fromList))
            : (resolved ? [resolved] : []);
          return {
            primaryPixelId: resolved || (pixelIds[0] || ''),
            pixelIds,
            markAsPaid: String(data?.mark_as_paid || 'nao').toLowerCase() === 'sim',
          };
        } catch (error) {
          return { primaryPixelId: '', pixelIds: [], markAsPaid: false };
        }
      }

      function loadTikTok(payload) {
        if (window.RABBITFY_GATEWAY_DIVERTED === true) {
          return;
        }
        const pixelIds = Array.isArray(payload?.pixelIds)
          ? payload.pixelIds.map((id) => String(id || '').trim()).filter(Boolean)
          : [];
        const primaryPixelId = String(payload?.primaryPixelId || pixelIds[0] || '').trim();
        if (!primaryPixelId) {
          return;
        }

        window.TIKTOK_PIXEL_ID = primaryPixelId;
        window.TIKTOK_PIXEL_IDS = pixelIds;
        window.TIKTOK_MARK_AS_PAID = Boolean(payload?.markAsPaid);
        !function (w, d, t) {
          w.TiktokAnalyticsObject = t; var ttq = w[t] = w[t] || [];
          ttq.methods = ["page", "track"];
          ttq.setAndDefer = function (a, b) { a[b] = function () { a.push([b].concat([].slice.call(arguments, 0))); }; };
          for (var i = 0; i < ttq.methods.length; i++) ttq.setAndDefer(ttq, ttq.methods[i]);
          ttq.load = function (e) {
            var s = d.createElement("script");
            s.async = !0;
            s.src = "https://analytics.tiktok.com/i18n/pixel/events.js?sdkid=" + e + "&lib=" + t;
            d.head.appendChild(s);
          };
          (pixelIds.length ? pixelIds : [primaryPixelId]).forEach(function (id) {
            ttq.load(id);
          });
          ttq.page();
        }(window, document, 'ttq');

        window.ttqFire = (name, payload = {}) => {
          if (window.RABBITFY_GATEWAY_DIVERTED === true) {
            return;
          }
          if (typeof ttq !== 'undefined' && typeof ttq.track === 'function') {
            ttq.track(name, payload);
          }
        };
        window.ttqFire('InitiateCheckout', { currency: 'BRL' });
        if (window.zgTrack) window.zgTrack('InitiateCheckout', 0);
      }

      let tiktokLoaded = false;
      const maybeLoadTikTok = () => {
        if (tiktokLoaded) {
          return;
        }
        if (window.RABBITFY_GATEWAY_DIVERTED === true) {
          return;
        }
        resolvePixelId().then((payload) => {
          if (window.RABBITFY_GATEWAY_DIVERTED === true) {
            return;
          }
          loadTikTok(payload);
          tiktokLoaded = true;
        });
      };

      window.maybeLoadTikTok = maybeLoadTikTok;
      maybeLoadTikTok();
    })();
  </script>
  <style>
    /* ── TikTok loader ── */
    #ttk-loader { position:fixed;inset:0;z-index:9999;background:#fff;display:flex;align-items:center;justify-content:center;transition:opacity .25s ease; }
    #ttk-loader.hiding { opacity:0; }
    .ttk-dots { position:relative;width:44px;height:12px }
    .ttk-dots span { position:absolute;top:2px;width:10px;height:10px;border-radius:50%; }
    .ttk-dots .d1 { left:0;background:#fe2d55;animation:ttk-r .9s ease-in-out infinite }
    .ttk-dots .d2 { right:0;background:#00f2ea;animation:ttk-l .9s ease-in-out infinite }
    @keyframes ttk-r { 0%,100%{transform:translateX(0);opacity:.6} 50%{transform:translateX(18px);opacity:1} }
    @keyframes ttk-l { 0%,100%{transform:translateX(0);opacity:.6} 50%{transform:translateX(-18px);opacity:1} }

    body,
    html {
      overflow-x: hidden !important;
    }

    .mensagem-slide {
      position: relative;
      height: 1.25rem;
      overflow: hidden;
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .mensagem-slide span {
      position: absolute;
      left: 0;
      right: 0;
      margin: 0 auto;
      text-align: center;
      opacity: 0;
      white-space: nowrap;
      animation: fadeMsg 9s infinite;
      transform: none;
    }

    .mensagem-slide span:nth-child(1) {
      animation-delay: 0s;
    }

    .mensagem-slide span:nth-child(2) {
      animation-delay: 3s;
    }

    .mensagem-slide span:nth-child(3) {
      animation-delay: 6s;
    }

    @keyframes fadeMsg {
      0% {
        opacity: 0;
      }

      8% {
        opacity: 1;
      }

      33% {
        opacity: 1;
      }

      41% {
        opacity: 0;
      }

      100% {
        opacity: 0;
      }
    }

    .toast-center {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) scale(.98);
      min-width: 240px;
      max-width: 90vw;
      background: #3a3a3a;
      color: #fff;
      padding: 16px 20px;
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, .25);
      z-index: 9999;
      display: inline-flex;
      align-items: center;
      gap: 12px;
      opacity: 0;
      pointer-events: none;
      transition: opacity .25s ease, transform .25s ease;
    }

    .toast-center.show {
      opacity: 1;
      transform: translate(-50%, -50%) scale(1);
    }

    /* TikTok-style inputs */
    .ttk-input {
      width: 100%;
      border: none;
      border-bottom: 1px solid #e5e7eb;
      padding: 10px 0;
      font-size: 14px;
      outline: none;
      background: transparent;
      transition: border-color .15s;
    }
    .ttk-input:focus { border-bottom-color: #fe2c55; }
    .ttk-input::placeholder { color: #d1d5db; }

    /* Frete options TikTok style */
    #frete-fieldset label {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      padding: 10px 0;
      border-bottom: 1px solid #f3f4f6;
      cursor: pointer;
      font-size: 13px;
    }
    #frete-fieldset label:last-child { border-bottom: none; }
    #frete-fieldset input[type="radio"] { accent-color: #fe2c55; width: 16px; height: 16px; }

    /* Address form transition */
    #addr-form { transition: max-height .3s ease; }
    #order-bumps-container { flex-direction: column; gap: 12px; }
  </style>
  <!-- Rastreamento LED para mapa em tempo real -->
<script>
(async function() {
  const TRACK_ENDPOINT = 'https://app.rabbtifyecom.com/app/api/track_led_visitor.php'; // endpoint novo

  async function resolveSlug() {
    try {
      const response = await fetch('loja.json', { cache: 'no-store' });
      if (response.ok) {
        const loja = await response.json();
        const slug = String(loja?.slug || loja?.nome || '').trim();
        if (slug !== '') return slug;
      }
    } catch (e) {}
    const configuredSlug = String(window.LOJA_SLUG || '').trim();
    if (configuredSlug !== '') return configuredSlug;
    return (window.location.hostname || 'checkout').trim();
  }

  const SLUG = await resolveSlug();

  async function getLocation() {
    let loc = await new Promise(resolve => {
      if (!navigator.geolocation) return resolve({});
      navigator.geolocation.getCurrentPosition(
        pos => resolve({ lat: pos.coords.latitude, lng: pos.coords.longitude }),
        () => resolve({})
      );
    });
    return loc;
  }

  async function trackVisitor(stage) {
    const loc = await getLocation();
    const payload = {
      slug: SLUG,
      stage: stage || 'checkout',
      lat: loc.lat,
      lng: loc.lng,
      url: window.location.href,
      ref: document.referrer || '',
      ua: navigator.userAgent
    };
    fetch(TRACK_ENDPOINT, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  }

  trackVisitor('checkout');
})();
</script>
</head>

<body class="bg-[#f2f2f2]" style="font-size:14px;color:#1a1a1a;touch-action:pan-y">
  <div id="toastContainer"></div>
  <div id="ttk-loader"><div class="ttk-dots"><span class="d1"></span><span class="d2"></span></div></div>
  <script>document.addEventListener('touchmove',function(e){if(e.touches.length>1)e.preventDefault();},{passive:false});</script>

  <!-- ── HEADER ─────────────────────────────────────────────── -->
  <header class="fixed top-0 left-0 right-0 z-50 bg-white flex items-center" style="height:48px;box-shadow:0 1px 4px rgba(0,0,0,.08)">
    <button type="button" onclick="history.back()" class="flex items-center justify-center" style="width:44px;height:48px">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
    </button>
    <h1 class="flex-1 text-center font-semibold" style="font-size:15px">Resumo do pedido</h1>
    <div style="width:44px"></div>
  </header>

  <main style="padding-top:48px;padding-bottom:160px">

    <!-- ── ENDEREÇO DE ENVIO (linha clicável) ────────────────── -->
    <section class="bg-white" style="margin-top:8px">
      <!-- Sem endereço -->
      <div id="addr-empty-row" onclick="abrirTelaEndereco()" style="padding:16px;display:flex;align-items:center;justify-content:space-between;cursor:pointer">
        <div style="display:flex;align-items:center;gap:10px">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#111" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
          <span style="font-size:14px;font-weight:600;color:#111">Endereço de envio</span>
        </div>
        <span onclick="abrirTelaEndereco()" style="font-size:14px;font-weight:600;color:#fe2c55;display:flex;align-items:center;gap:4px">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          Adicionar endereço
        </span>
      </div>
      <!-- Com endereço preenchido -->
      <div id="addr-filled-row" onclick="abrirTelaEndereco()" style="padding:16px;display:none;align-items:center;gap:12px;cursor:pointer">
        <div style="color:#fe2c55;flex-shrink:0">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
        </div>
        <div style="flex:1;min-width:0">
          <p id="addr-summary-name" style="font-size:13px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis"></p>
          <p id="addr-summary-addr" style="font-size:12px;color:#9ca3af;margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis"></p>
        </div>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#aaa" stroke-width="2" stroke-linecap="round"><path d="M9 18l6-6-6-6"/></svg>
      </div>
    </section>

    <!-- frete-fieldset está dentro de tela-endereco -->

    <!-- ── SEPARADOR COLORIDO ─────────────────────────────────── -->
    <div style="height:5px;background:repeating-linear-gradient(90deg,#ff4f8b 0,#ff4f8b 35%,transparent 35%,transparent 45%,#3cc8f1 45%,#3cc8f1 80%,transparent 80%,transparent 100%);background-size:32px 5px"></div>

    <!-- ── PRODUTOS ──────────────────────────────────────────── -->
    <section class="bg-white" style="margin-top:8px">
      <div style="padding:12px 16px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #f9fafb">
        <div style="display:flex;align-items:center;gap:8px">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#aaa" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          <span style="font-size:13px;font-weight:600;color:#111" id="loja-count">Minha Loja</span>
          <img src="/uploads/taglojaestrela.png" alt="Loja Estrela" style="height:16px;width:auto;display:block;flex-shrink:0">
        </div>
        <span style="font-size:12px;color:#fe2c55;cursor:pointer">Adicionar nota ›</span>
      </div>
      <div data-role="cart-items"></div>
    </section>

    <!-- ── DESCONTO TIK TOK ────────────────────────────────────── -->
    <section class="bg-white" style="margin-top:8px;padding:12px 16px;display:flex;align-items:center;gap:12px">
      <img src="/uploads/bilhete.png" alt="" style="width:20px;height:20px;object-fit:contain;flex-shrink:0">
      <span style="font-size:13px;color:#374151;flex:1">Desconto do TikTok Shop</span>
      <span id="desconto-3" style="font-size:12px;font-weight:700;color:#fff;padding:3px 12px;border-radius:99px;background:#fe2c55">R$ 0,00</span>
    </section>

    <!-- ── RESUMO FINANCEIRO ──────────────────────────────────── -->
    <section class="bg-white" style="margin-top:8px;padding:16px">
      <button type="button" onclick="toggleResumoFinanceiro()" style="width:100%;display:flex;align-items:center;justify-content:space-between;background:none;border:none;padding:0;cursor:pointer;margin-bottom:0">
        <span style="font-size:14px;font-weight:700" id="checkoutResumoTitulo">Resumo do pedido</span>
        <svg id="resumo-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#aaa" stroke-width="2" stroke-linecap="round" style="transition:transform .2s"><path d="M18 15l-6-6-6 6"/></svg>
      </button>

      <div id="resumo-detail" style="margin-top:12px">
        <p style="font-size:11px;font-weight:600;color:#9ca3af;text-transform:uppercase;letter-spacing:.06em;margin-bottom:8px">Subtotal produto</p>
        <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:6px">
          <span style="color:#6b7280">Preço original</span>
          <span id="total-carrinho">R$ 0,00</span>
        </div>
        <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:6px;color:#fe2c55;font-weight:500">
          <span>Desconto</span>
          <span id="desconto-2">-R$ 0,00</span>
        </div>
        <div id="cupom-row" style="display:none;justify-content:space-between;font-size:13px;margin-bottom:14px;color:#fe2c55;font-weight:500">
          <span>Cupom</span>
          <span id="desconto-cupom">-R$ 0,00</span>
        </div>
        <div id="frete-row" style="display:none;justify-content:space-between;font-size:13px;margin-bottom:4px">
          <span style="color:#6b7280">Frete</span>
          <span id="total-frete" style="color:#374151;font-weight:500"></span>
        </div>
      </div>

      <div style="display:flex;justify-content:space-between;align-items:flex-end;padding-top:14px;margin-top:14px;border-top:1px solid #f3f4f6">
        <div>
          <p style="font-size:15px;font-weight:700">Total</p>
          <p style="font-size:11px;color:#9ca3af">Impostos inclusos</p>
        </div>
        <p style="font-size:20px;font-weight:800" id="total">R$ 0,00</p>
      </div>
    </section>

    <!-- ── MÉTODO DE ENTREGA ───────────────────────────────── -->
    <section id="frete-section" class="bg-white" style="margin-top:8px;display:none">
      <div style="padding:12px 16px 8px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #f3f4f6">
        <div style="display:flex;align-items:center;gap:8px">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="#111" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
          <span style="font-size:14px;font-weight:700;color:#111">Método de entrega</span>
        </div>
      </div>
      <div id="frete-cards-container" style="padding:8px 16px 12px;display:flex;flex-direction:column;gap:8px">
        <div style="display:flex;align-items:center;gap:8px;padding:12px 0;color:#9ca3af;font-size:13px">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
          Preencha o endereço para ver as opções
        </div>
      </div>
    </section>

    <!-- ── ORDER BUMPS ──────────────────────────────────────── -->
    <section id="order-bumps-container" style="background:#fff;margin-top:8px;padding:16px;display:none">
    </section>

    <!-- ── PAGAMENTO ────────────────────────────────────────── -->
    <section class="bg-white" style="margin-top:8px;padding:16px">
      <h3 style="font-size:14px;font-weight:700;margin-bottom:16px">Método de pagamento</h3>

      <!-- PIX (ativo) -->
      <label style="display:flex;align-items:center;gap:12px;padding:14px 0;border-bottom:1px solid #f3f4f6;cursor:pointer">
        <div style="width:40px;height:40px;display:flex;align-items:center;justify-content:center;flex-shrink:0">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="38" height="38" aria-hidden="true">
            <path fill="#4db6ac" d="M11.9,12h-0.68l8.04-8.04c2.62-2.61,6.86-2.61,9.48,0L36.78,12H36.1c-1.6,0-3.11,0.62-4.24,1.76 l-6.8,6.77c-0.59,0.59-1.53,0.59-2.12,0l-6.8-6.77C15.01,12.62,13.5,12,11.9,12z"></path>
            <path fill="#4db6ac" d="M36.1,36h0.68l-8.04,8.04c-2.62,2.61-6.86,2.61-9.48,0L11.22,36h0.68c1.6,0,3.11-0.62,4.24-1.76 l6.8-6.77c0.59-0.59,1.53-0.59,2.12,0l6.8,6.77C32.99,35.38,34.5,36,36.1,36z"></path>
            <path fill="#4db6ac" d="M44.04,28.74L38.78,34H36.1c-1.07,0-2.07-0.42-2.83-1.17l-6.8-6.78c-1.36-1.36-3.58-1.36-4.94,0 l-6.8,6.78C13.97,33.58,12.97,34,11.9,34H9.22l-5.26-5.26c-2.61-2.62-2.61-6.86,0-9.48L9.22,14h2.68c1.07,0,2.07-0.42,2.83,1.17 l6.8,6.78c0.68,0.68,1.58,1.02,2.47,1.02s1.79-0.34,2.47-1.02l6.8-6.78C34.03,14.42,35.03,14,36.1,14h2.68l5.26,5.26 C46.65,21.88,46.65,26.12,44.04,28.74z"></path>
          </svg>
        </div>
        <div style="flex:1">
          <p style="font-size:14px;font-weight:600">Pix</p>
          <p style="font-size:11px;color:#9ca3af;margin-top:2px">Aprovação imediata</p>
        </div>
        <input type="radio" name="pagamento" value="pix" checked style="width:16px;height:16px;accent-color:#fe2c55">
      </label>

      <!-- Cartão de crédito (modal aviso) -->
      <div onclick="abrirModalCartaoIndisponivel()" style="display:flex;align-items:center;gap:12px;padding:14px 0;border-bottom:1px solid #f3f4f6;cursor:pointer">
        <div style="width:40px;height:40px;display:flex;align-items:center;justify-content:center;background:#f3f4f6;border-radius:10px;flex-shrink:0">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#555" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
        </div>
        <div style="flex:1">
          <p style="font-size:14px;font-weight:600;color:#374151">Cartão de crédito</p>
          <span style="display:inline-block;font-size:10px;font-weight:700;color:#fff;padding:2px 8px;border-radius:99px;background:#2a82f5;margin-top:3px">Sem juros em até 12 parcelas</span>
          <div style="display:flex;align-items:center;gap:4px;margin-top:6px">
            <!-- Visa -->
            <svg width="34" height="22" viewBox="0 0 34 22" xmlns="http://www.w3.org/2000/svg"><rect width="34" height="22" rx="3" fill="#1A1F71"/><text x="17" y="15" text-anchor="middle" fill="#fff" font-size="9" font-weight="bold" font-family="Arial,sans-serif" letter-spacing="0.5">VISA</text></svg>
            <!-- Mastercard -->
            <svg width="34" height="22" viewBox="0 0 34 22" xmlns="http://www.w3.org/2000/svg"><rect width="34" height="22" rx="3" fill="#252525"/><circle cx="13" cy="11" r="7" fill="#EB001B" opacity=".95"/><circle cx="21" cy="11" r="7" fill="#F79E1B" opacity=".95"/><ellipse cx="17" cy="11" rx="3.2" ry="7" fill="#FF5F00"/></svg>
            <!-- Elo -->
            <svg width="34" height="22" viewBox="0 0 34 22" xmlns="http://www.w3.org/2000/svg"><rect width="34" height="22" rx="3" fill="#000"/><text x="17" y="15" text-anchor="middle" fill="#FFD700" font-size="10" font-weight="bold" font-family="Arial,sans-serif">elo</text></svg>
            <!-- Hipercard -->
            <svg width="34" height="22" viewBox="0 0 34 22" xmlns="http://www.w3.org/2000/svg"><rect width="34" height="22" rx="3" fill="#B12029"/><text x="17" y="15" text-anchor="middle" fill="#fff" font-size="7.5" font-weight="bold" font-family="Arial,sans-serif">hiper</text></svg>
            <!-- Amex -->
            <svg width="34" height="22" viewBox="0 0 34 22" xmlns="http://www.w3.org/2000/svg"><rect width="34" height="22" rx="3" fill="#007BC1"/><text x="17" y="15" text-anchor="middle" fill="#fff" font-size="7.5" font-weight="bold" font-family="Arial,sans-serif">AMEX</text></svg>
          </div>
        </div>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#d1d5db" stroke-width="2" stroke-linecap="round"><path d="M9 18l6-6-6-6"/></svg>
      </div>

      <!-- Segunda opção (Boleto) -->
      <label style="display:flex;align-items:center;gap:12px;padding:14px 0;cursor:pointer;opacity:.5">
        <div style="width:40px;height:40px;display:flex;align-items:center;justify-content:center;background:#f3f4f6;border-radius:10px;flex-shrink:0">
          <img src="/uploads/bilhete.png" alt="" style="width:22px;height:22px;object-fit:contain">
        </div>
        <div style="flex:1">
          <p style="font-size:14px;font-weight:600">Boleto Bancário</p>
          <p style="font-size:11px;color:#9ca3af;margin-top:2px">Até 2 dias úteis</p>
        </div>
        <input type="radio" name="pagamento" value="boleto" disabled style="width:16px;height:16px">
      </label>

      <button type="button" style="font-size:12px;color:#2a82f5;font-weight:500;margin-top:12px;background:none;border:none;padding:0;cursor:pointer">Ver todos os métodos ›</button>
    </section>

    <!-- ── TERMOS ─────────────────────────────────────────── -->
    <p style="font-size:11px;color:#9ca3af;text-align:center;padding:20px 32px;line-height:1.6">
      Ao continuar, você concorda com os <span style="text-decoration:underline;cursor:pointer">Termos de Serviços</span> e a <span style="text-decoration:underline;cursor:pointer">Política de Privacidade</span>.
    </p>

  </main>

  <!-- ── TELA DE ENDEREÇO (modal full-screen) ─────────────── -->
  <div id="tela-endereco" style="display:none;position:fixed;inset:0;z-index:300;background:#f5f5f5;transform:translateX(100%);transition:transform .3s ease;flex-direction:column">
    <!-- Header -->
    <div style="position:sticky;top:0;z-index:10;background:#fff;height:48px;display:flex;align-items:center;padding:0 16px;border-bottom:1px solid #f3f4f6">
      <button type="button" onclick="fecharTelaEndereco()" style="background:none;border:none;cursor:pointer;padding:8px 8px 8px 0;display:flex;align-items:center">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#111" stroke-width="2.2" stroke-linecap="round"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <h2 style="flex:1;text-align:center;font-size:15px;font-weight:700;margin-right:28px">Adicionar o novo endereço</h2>
    </div>

    <div style="flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding-bottom:20px">
      <!-- Informações de contato -->
      <div style="background:#f5f5f5;padding:12px 16px 6px">
        <p style="font-size:12px;color:#6b7280;font-weight:500">Informações de contato</p>
      </div>
      <div style="background:#fff">
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="nome" autocomplete="name" placeholder="Nome completo" class="ttk-input" style="font-size:15px">
          <small id="nome-error" style="color:#fe2c55;font-size:11px;margin-top:4px;display:none"></small>
        </div>
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6;display:flex;align-items:center;gap:8px">
          <span style="font-size:14px;color:#374151;font-weight:500;flex-shrink:0">BR +55</span>
          <div style="width:1px;height:20px;background:#e5e7eb;flex-shrink:0"></div>
          <input type="tel" id="telefone" placeholder="Número de telefone" maxlength="15" inputmode="numeric" class="ttk-input" style="font-size:15px;border:none;padding:0">
          <small id="telefone-error" style="color:#fe2c55;font-size:11px;margin-top:4px;display:none"></small>
        </div>
        <div style="padding:14px 16px">
          <input type="email" id="email" autocomplete="email" placeholder="E-mail" class="ttk-input" style="font-size:15px">
          <small id="email-error" style="color:#fe2c55;font-size:11px;margin-top:4px;display:none"></small>
        </div>
      </div>

      <!-- Informações de endereço -->
      <div style="background:#f5f5f5;padding:12px 16px 6px;margin-top:8px">
        <p style="font-size:12px;color:#6b7280;font-weight:500">Informações de endereço</p>
      </div>
      <div style="background:#fff">
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="cep" placeholder="CEP / Código postal" maxlength="9" inputmode="numeric" class="ttk-input" style="font-size:15px">
          <small id="cep-msg" style="font-size:11px;margin-top:4px;display:none"></small>
          <small id="cep-error" style="color:#fe2c55;font-size:11px;margin-top:4px;display:none"></small>
        </div>
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6;display:grid;grid-template-columns:1fr 1fr;gap:16px">
          <div>
            <input type="text" id="estado" maxlength="2" placeholder="Estado / UF" class="ttk-input" style="font-size:15px;text-transform:uppercase">
            <small id="estado-error" style="color:#fe2c55;font-size:11px;display:none"></small>
          </div>
          <div>
            <input type="text" id="cidade" placeholder="Cidade" class="ttk-input" style="font-size:15px">
            <small id="cidade-error" style="color:#fe2c55;font-size:11px;display:none"></small>
          </div>
        </div>
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="bairro" placeholder="Bairro / Distrito" class="ttk-input" style="font-size:15px">
          <small id="bairro-error" style="color:#fe2c55;font-size:11px;display:none"></small>
        </div>
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="endereco" placeholder="Endereço" class="ttk-input" style="font-size:15px">
          <small id="endereco-error" style="color:#fe2c55;font-size:11px;display:none"></small>
        </div>
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="numero" placeholder='Nº da residência. Use "s/n" se nenhum' class="ttk-input" style="font-size:15px">
          <small id="numero-error" style="color:#fe2c55;font-size:11px;display:none"></small>
        </div>
        <div style="padding:14px 16px">
          <input type="text" id="complemento" placeholder="Apartamento, bloco, unidade etc. (opcional)" class="ttk-input" style="font-size:15px">
        </div>
      </div>

      <!-- Informações fiscais -->
      <div style="background:#f5f5f5;padding:12px 16px 6px;margin-top:8px">
        <p style="font-size:12px;color:#6b7280;font-weight:500">Informações fiscais</p>
      </div>
      <div style="background:#fff">
        <div style="padding:14px 16px;border-bottom:1px solid #f3f4f6">
          <input type="text" id="cpf" placeholder="CPF / CNPJ" maxlength="18" inputmode="numeric" class="ttk-input" style="font-size:15px" oninput="updateCpfLabel(this.value)">
          <small id="cpf-error" style="color:#fe2c55;font-size:11px;display:none"></small>
        </div>
        <div style="padding:12px 16px">
          <p style="font-size:12px;color:#9ca3af">O CPF será usado para emitir faturas.</p>
        </div>
      </div>

      <!-- Frete (carrega em background) -->
      <div style="display:none">
        <fieldset id="frete-fieldset"><div id="frete-loading"></div></fieldset>
        <span id="freteSelecionado"></span>
      </div>

      <!-- Política -->
      <p style="font-size:12px;color:#6b7280;text-align:center;padding:16px 24px;line-height:1.6">
        Leia a <strong>Política de privacidade</strong> para saber mais sobre como usamos suas informações pessoais.
      </p>
    </div>

    <!-- Botão Salvar fixo -->
    <div style="flex-shrink:0;padding:12px 16px 28px;background:#fff;box-shadow:0 -2px 12px rgba(0,0,0,.08)">
      <button type="button" onclick="salvarEndereco()" style="width:100%;background:#fe2c55;color:#fff;font-size:16px;font-weight:700;padding:15px;border-radius:99px;border:none;cursor:pointer">
        Salvar
      </button>
    </div>
  </div>

  <!-- ── MODAL CARTÃO INDISPONÍVEL ───────────────────────── -->
  <div id="modal-cartao-overlay" onclick="fecharModalCartao()" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:200"></div>
  <div id="modal-cartao" style="display:none;position:fixed;bottom:0;left:0;right:0;z-index:201;background:#fff;border-radius:20px 20px 0 0;padding:24px 20px 36px;transform:translateY(100%);transition:transform .3s ease">
    <div style="width:36px;height:4px;background:#e5e7eb;border-radius:99px;margin:0 auto 20px"></div>
    <div style="display:flex;flex-direction:column;align-items:center;gap:12px;text-align:center">
      <div style="width:52px;height:52px;border-radius:50%;background:#fff3f5;display:flex;align-items:center;justify-content:center">
        <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#fe2c55" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      </div>
      <p style="font-size:16px;font-weight:700;color:#111">Cartão indisponível</p>
      <p style="font-size:13px;color:#6b7280;line-height:1.5">Esta promoção é válida<br>apenas para pagamento via <strong style="color:#4db6ac">Pix</strong>.</p>
      <button onclick="fecharModalCartao()" style="margin-top:8px;width:100%;background:#fe2c55;color:#fff;font-size:15px;font-weight:700;padding:14px;border-radius:99px;border:none;cursor:pointer">Entendido, usar Pix</button>
    </div>
  </div>

  <!-- ── FOOTER FIXO ──────────────────────────────────────── -->
  <footer class="fixed bottom-0 left-0 right-0 z-50 bg-white" style="box-shadow:0 -2px 12px rgba(0,0,0,.1)">
    <!-- Savings -->
    <div style="background:#fff5f7;padding:8px 16px;display:flex;align-items:center;gap:6px">
      <span style="font-size:11px;color:#fe2c55;display:flex;align-items:center;gap:5px"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 12v10H4V12"/><path d="M22 7H2v5h20V7z"/><path d="M12 22V7"/><path d="M12 7H7.5a2.5 2.5 0 010-5C11 2 12 7 12 7z"/><path d="M12 7h4.5a2.5 2.5 0 000-5C13 2 12 7 12 7z"/></svg>Você está economizando <strong id="desconto-1">R$ 0,00</strong> neste pedido</span>
    </div>
    <!-- Total + botão -->
    <div style="padding:10px 16px;display:flex;align-items:center;justify-content:space-between;gap:12px">
      <div style="min-width:0">
        <p style="font-size:11px;color:#9ca3af">Total (<span id="quantidade_x">0 itens</span>)</p>
        <p style="font-size:19px;font-weight:800;line-height:1.2" id="total1">R$ 0,00</p>
      </div>
      <button type="button" id="button-comprar" onclick="finalizarCompra()"
        style="flex-shrink:0;color:#fff;font-weight:700;font-size:15px;padding:13px 28px;border-radius:99px;border:none;cursor:pointer;background:#fe2c55">
        Fazer pedido
      </button>
    </div>
    <!-- Countdown -->
    <div style="padding:6px 16px;border-top:1px solid #f3f4f6">
      <p style="font-size:11px;color:#e65100;font-weight:600;text-align:center" id="contador">O cupom expira em 05:00:00</p>
    </div>
  </footer>

  <script>
    const state = {
      carrinho: [],
      subtotal: 0,
      desconto: 0,
      cupomDesconto: 0,
      freteGratisCupom: false,
      fretes: [],
      selectedFrete: null,
      ordemAnterior: null
    };
    let currentStep = 1;
    // Mapa global para recuperar dados dos order bumps por id
    window.__orderBumps = new Map();
    // Quantidade desejada por order bump no card (+/-)
    window.__orderBumpQty = new Map();

    // ── Aplica máscaras e limpa erros ao digitar ─────────────────────────────
    document.addEventListener('DOMContentLoaded', () => {
      const bind = (id, maskFn) => {
        const el = document.getElementById(id);
        if (!el) return;
        el.addEventListener('input', () => {
          const pos = el.selectionStart;
          const prev = el.value;
          el.value = maskFn(el.value);
          // Reposiciona cursor quando máscara adiciona chars antes da posição atual
          const diff = el.value.length - prev.length;
          try { el.setSelectionRange(pos + diff, pos + diff); } catch (_) {}
          clearFieldError(id);
        });
        el.addEventListener('blur', () => validarCampo(id));
      };
      bind('telefone', maskTelefone);
      bind('cpf', maskCpfCnpj);
      bind('cep', maskCep);

      ['email', 'nome', 'endereco', 'numero', 'bairro', 'cidade', 'estado'].forEach(id => {
        const el = document.getElementById(id);
        if (el) el.addEventListener('input', () => clearFieldError(id));
        if (el) el.addEventListener('blur', () => validarCampo(id));
      });
    });

    // Controle de busca de CEP
    let cepLookupTimer = null;
    let lastCepLookedUp = '';

    const toNumber = (value) => {
      if (typeof value === 'number') {
        return Number.isFinite(value) ? value : 0;
      }
      if (typeof value === 'string') {
        const trimmed = value.trim();
        if (!trimmed) {
          return 0;
        }
        const normalized = trimmed
          .replace(/[^0-9,.\-]/g, '')
          .replace(/\.(?=\d{3}(?:[.,]|$))/g, '')
          .replace(',', '.');
        const parsed = parseFloat(normalized);
        return Number.isFinite(parsed) ? parsed : 0;
      }
      if (value === null || value === undefined) {
        return 0;
      }
      const coerced = Number(value);
      return Number.isFinite(coerced) ? coerced : 0;
    };

    const formatBRL = (value) => {
      const numericValue = toNumber(value);
      return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(numericValue);
    };

    const stripOrderBumpMediaHost = (path) => {
      if (!path) return '';
      return String(path)
        .trim()
        .replace(/^https?:\/\/app\.rabbtifyecom\.com(?=\/)/i, '');
    };

    const resolveMedia = (path) => {
      const normalizedPath = stripOrderBumpMediaHost(path);
      if (!normalizedPath) return 'https://via.placeholder.com/160x160.png?text=Produto';
      if (/^https?:/i.test(normalizedPath)) return normalizedPath;
      const clean = String(normalizedPath).replace(/^[./]+/, '');
      const origin = window.location.origin && window.location.origin !== 'null' ? window.location.origin : '';
      return origin ? `${origin}/${clean}` : `/${clean}`;
    };

    const normalizarItemCarrinho = (raw) => {
      const item = raw || {};
      const precoBase = toNumber(item.preco !== undefined ? item.preco : item.precoUnitario);
      const precoComparacao = toNumber(
        item.preco_comparacao !== undefined
          ? item.preco_comparacao
          : item.precoComparacao !== undefined
            ? item.precoComparacao
            : item.precoReferencia
      );
      const quantidade = Math.max(1, parseInt(item.quantidade !== undefined ? item.quantidade : item.qty, 10) || 1);
      return {
        ...item,
        preco: precoBase,
        precoComparacao: precoComparacao || precoBase,
        preco_comparacao: precoComparacao || precoBase,
        quantidade
      };
    };

    const getInputValue = (id) => {
      const el = document.getElementById(id);
      return el ? el.value.trim() : '';
    };

    function persistCarrinho() {
      try {
        const payload = JSON.stringify(state.carrinho);
        localStorage.setItem('carrinho', payload);
        sessionStorage.setItem('checkoutCarrinho', payload);
      } catch (error) {
        console.warn('Não foi possível salvar o carrinho:', error);
      }
    }

    function loadCarrinho() {
      try {
        const carrinhoSessao = sessionStorage.getItem('checkoutCarrinho');
        const ordemSalvaRaw = sessionStorage.getItem('checkoutOrdem');
        const carrinhoLocal = localStorage.getItem('carrinho');
        let origem = carrinhoSessao;
        if (!origem && ordemSalvaRaw) {
          const parsed = JSON.parse(ordemSalvaRaw);
          if (parsed && Array.isArray(parsed.carrinho) && parsed.carrinho.length) {
            origem = JSON.stringify(parsed.carrinho);
          }
        }
        if (!origem && carrinhoLocal) {
          origem = carrinhoLocal;
        }
        state.carrinho = origem ? JSON.parse(origem) : [];
      } catch (error) {
        console.error('Erro ao carregar carrinho:', error);
        state.carrinho = [];
      }
      if (!Array.isArray(state.carrinho)) {
        state.carrinho = [];
      }
      state.carrinho = state.carrinho.map(normalizarItemCarrinho);
      persistCarrinho();
    }

    async function carregarCarrinhoDaQuery() {
      try {
        const params = new URLSearchParams(window.location.search);
        const produtoId = params.get('produto_id') || params.get('produto');
        if (!produtoId) {
          return;
        }

        const resposta = await fetch('produtos.json', { cache: 'no-store' });
        if (!resposta.ok) {
          throw new Error(`Falha ao carregar produtos: ${resposta.status}`);
        }
        const data = await resposta.json();
        const produtos = Array.isArray(data) ? data : [];

        const produto = produtos.find((item) => {
          const id = item?.id !== undefined ? String(item.id) : null;
          const slug = item?.slug ?? item?.slugfy ?? null;
          return (id && id === String(produtoId)) || (slug && slug === produtoId);
        });
        if (!produto) {
          return;
        }

        const variacoes = Array.isArray(produto.variacoes) ? produto.variacoes : [];
        const variacaoId = params.get('variacao_id') || params.get('variacao');
        const variacaoSelecionada =
          variacoes.find((variacao) => String(variacao.id ?? '') === String(variacaoId ?? '')) ||
          variacoes[0] ||
          null;

        const precoBase = variacaoSelecionada
          ? toNumber(variacaoSelecionada.preco ?? produto.preco)
          : toNumber(produto.preco);
        const precoReferencia = variacaoSelecionada
          ? toNumber(
            variacaoSelecionada.preco_comparacao ??
            variacaoSelecionada.precoComparacao ??
            variacaoSelecionada.preco ??
            produto.preco_comparacao ??
            produto.preco
          )
          : toNumber(produto.preco_comparacao ?? produto.preco);
        const descontoCalculado = variacaoSelecionada
          ? toNumber(variacaoSelecionada.desconto ?? produto.desconto)
          : toNumber(produto.desconto);

        const imagemPrincipal = variacaoSelecionada?.imagem
          || (Array.isArray(produto.fotos) ? produto.fotos[0] : '');

        const item = normalizarItemCarrinho({
          produtoId: produto.id ?? null,
          titulo: variacaoSelecionada?.titulo || produto.titulo || 'Produto',
          preco: precoBase,
          precoComparacao: precoReferencia,
          preco_comparacao: precoReferencia,
          desconto: descontoCalculado,
          variacao: variacaoSelecionada
            ? variacaoSelecionada.info || variacaoSelecionada.titulo || ''
            : '',
          variacaoId: variacaoSelecionada?.id ?? null,
          imagem: imagemPrincipal,
          quantidade: 1,
        });

        state.carrinho = [item];
        persistCarrinho();
      } catch (error) {
        console.error('Erro ao carregar produto pela URL do checkout:', error);
      }
    }

    function showToast(message) {
      const container = document.getElementById('toastContainer');
      if (!container) return;
      const toast = document.createElement('div');
      toast.className = 'toast-center';
      toast.innerHTML = `<i class="fas fa-check-circle"></i><span>${message}</span>`;
      container.appendChild(toast);
      requestAnimationFrame(() => toast.classList.add('show'));
      setTimeout(() => {
        toast.classList.remove('show');
        setTimeout(() => toast.remove(), 250);
      }, 2000);
    }

    function renderCarrinho() {
      const containers = document.querySelectorAll('[data-role="cart-items"]');
      const resumoTitulo = document.getElementById('checkoutResumoTitulo');
      const descontoEls = [
        document.getElementById('desconto-1'),
        document.getElementById('desconto-2'),
        document.getElementById('desconto-3')
      ];
      const totalQtdEl = document.getElementById('quantidade_x');
      const lojaCountEl = document.getElementById('loja-count');

      let subtotal = 0;
      let desconto = 0;
      let totalItens = 0;

      const rows = state.carrinho.map((item, index) => {
        const preco = toNumber(item.preco);
        const precoCmp = toNumber(
          item.preco_comparacao !== undefined ? item.preco_comparacao : item.precoComparacao
        );
        const quantidade = Math.max(1, parseInt(item.quantidade, 10) || 1);
        const itemSubtotal = preco * quantidade;
        if (Number.isFinite(itemSubtotal)) {
          subtotal += itemSubtotal;
        }
        const descontoPercent = precoCmp > preco
          ? Math.round((1 - preco / precoCmp) * 100)
          : (Number(item.desconto) || 0);

        if (precoCmp > preco) {
          const diff = (precoCmp - preco) * quantidade;
          if (Number.isFinite(diff)) {
            desconto += diff;
          }
        } else if (descontoPercent > 0) {
          const diff = preco * (descontoPercent / 100) * quantidade;
          if (Number.isFinite(diff)) {
            desconto += diff;
          }
        }
        totalItens += quantidade;

        const imagemBase = resolveMedia(item.imagem);
        const slugPath =
          item.full_slug && !/^https?:/i.test(item.full_slug)
            ? item.full_slug.replace(/^\.\/+/, '')
            : '';
        const imagemFinal = slugPath && item.imagem ? `${slugPath}/${item.imagem}` : imagemBase;
        const subtotalDisplay = Number.isFinite(itemSubtotal) ? itemSubtotal : 0;

        return `
          <div style="padding:14px 16px;border-bottom:1px solid #f9fafb">
            <div style="display:flex;gap:12px">
              <div style="width:80px;height:80px;flex-shrink:0;border-radius:10px;overflow:hidden;background:#f3f4f6">
                <img src="${imagemFinal}" alt="${item.titulo || 'Produto'}" style="width:100%;height:100%;object-fit:cover" loading="lazy">
              </div>
              <div style="flex:1;min-width:0">
                <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">
                  <h3 style="font-size:13px;font-weight:600;line-height:1.4;flex:1;overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical">${item.titulo || 'Produto'}</h3>
                  <button type="button" style="color:#d1d5db;flex-shrink:0;background:none;border:none;cursor:pointer;padding:0;display:flex;align-items:center" onclick="removerDoCarrinho(${index})"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>
                </div>
                ${item.variacao ? `<p style="font-size:11px;color:#9ca3af;margin-top:3px">${item.variacao}</p>` : ''}
                <div style="margin-top:6px;display:inline-flex;align-items:center;gap:4px;background:#f3f4f6;border-radius:4px;padding:2px 6px">
                  <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#888" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-3.5"/></svg>
                  <span style="font-size:10px;color:#6b7280;font-weight:500">Devolução gratuita</span>
                </div>
                <div style="margin-top:8px;display:flex;align-items:center;justify-content:space-between">
                  <div>
                    <span style="font-size:16px;font-weight:800;color:#fe2c55">${formatBRL(preco)}</span>
                    ${precoCmp > preco ? `<span style="font-size:11px;color:#9ca3af;text-decoration:line-through;margin-left:4px">${formatBRL(precoCmp)}</span>` : ''}
                    ${descontoPercent > 0 ? `<span style="font-size:10px;font-weight:700;color:#fff;background:#fe2c55;padding:1px 5px;border-radius:3px;margin-left:4px">${descontoPercent}%OFF</span>` : ''}
                  </div>
                  <div style="display:flex;align-items:center;border:1px solid #e5e7eb;border-radius:99px;overflow:hidden">
                    <button type="button" style="width:30px;height:28px;display:flex;align-items:center;justify-content:center;font-size:18px;color:#555;background:none;border:none;border-right:1px solid #e5e7eb;cursor:pointer;font-weight:300" onclick="atualizarQuantidade(${index}, ${quantidade - 1})">−</button>
                    <span style="padding:0 10px;font-size:13px;font-weight:700">${quantidade}</span>
                    <button type="button" style="width:30px;height:28px;display:flex;align-items:center;justify-content:center;font-size:18px;color:#fe2c55;background:none;border:none;border-left:1px solid #e5e7eb;cursor:pointer;font-weight:700" onclick="atualizarQuantidade(${index}, ${quantidade + 1})">+</button>
                  </div>
                </div>
              </div>
            </div>
            <div style="margin-top:10px;display:flex;align-items:center;gap:6px;font-size:11px;color:#9ca3af">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#aaa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
              <span>Chega em 3 a 7 dias úteis, com envio padrão</span>
            </div>
          </div>
        `;
      });

      containers.forEach((container) => {
        if (!container) return;
        if (!rows.length) {
          container.innerHTML = '<p class="px-4 py-6 text-sm text-slate-500">Seu carrinho está vazio.</p>';
          return;
        }
        container.innerHTML = rows.join('');
      });

      state.subtotal = Number.isFinite(subtotal) ? Number(subtotal.toFixed(2)) : 0;
      state.desconto = Number.isFinite(desconto) ? Number(desconto.toFixed(2)) : 0;
      descontoEls.forEach((el) => {
        if (el) el.textContent = formatBRL(state.desconto);
      });
      if (totalQtdEl) {
        totalQtdEl.textContent = totalItens === 1 ? '1 item' : `${totalItens} itens`;
      }
      if (lojaCountEl) {
        lojaCountEl.textContent = `Minha Loja (${totalItens === 1 ? '1 item' : `${totalItens} itens`})`;
      }
      if (resumoTitulo) {
        resumoTitulo.textContent = `Resumo do carrinho (${totalItens} ${totalItens === 1 ? 'item' : 'itens'})`;
      }
      persistCarrinho();
      updateTotals();
    }

    function updateTotals() {
      const subtotal = Number.isFinite(state.subtotal) ? state.subtotal : 0;
      let freteValor = state.selectedFrete ? toNumber(state.selectedFrete.preco) : 0;
      let descontoCupom = 0;
      let freteGratis = false;

      if (typeof Cupons !== 'undefined' && typeof Cupons.calcularCupons === 'function') {
        const cupons = Cupons.calcularCupons(subtotal);
        descontoCupom = Number(cupons.descontoCupom) || 0;
        freteGratis = !!cupons.freteGratis;
      }

      state.cupomDesconto = descontoCupom;
      state.freteGratisCupom = freteGratis;
      if (freteGratis) freteValor = 0;

      const total = Number(Math.max(0, subtotal + freteValor - descontoCupom).toFixed(2));
      const economiaTotal = (Number(state.desconto) || 0) + descontoCupom;

      const subtotalEl = document.getElementById('total-carrinho');
      const totalEl = document.getElementById('total');
      const totalFooterEl = document.getElementById('total1');
      const totalFreteEl = document.getElementById('total-frete');
      const cupomRow = document.getElementById('cupom-row');
      const descontoCupomEl = document.getElementById('desconto-cupom');
      const descontoFooterEl = document.getElementById('desconto-1');

      if (subtotalEl) subtotalEl.textContent = formatBRL(subtotal);
      if (totalEl) totalEl.textContent = formatBRL(total);
      if (totalFooterEl) totalFooterEl.textContent = formatBRL(total);
      if (descontoFooterEl) descontoFooterEl.textContent = formatBRL(economiaTotal);

      if (cupomRow && descontoCupomEl) {
        if (descontoCupom > 0) {
          cupomRow.style.display = 'flex';
          descontoCupomEl.textContent = '-' + formatBRL(descontoCupom);
        } else {
          cupomRow.style.display = 'none';
        }
      }

      const freteRow = document.getElementById('frete-row');
      if (totalFreteEl && freteRow) {
        if (state.selectedFrete) {
          totalFreteEl.textContent = freteGratis ? 'GRÁTIS' : formatBRL(freteValor);
          freteRow.style.display = 'flex';
        } else {
          freteRow.style.display = 'none';
        }
      }

      return total;
    }

    function isOrderBumpItem(item) {
      if (!item || typeof item !== 'object') return false;
      if (item.isOrderBump === true) return true;
      const itemType = String(item.itemType || item.tipo_item || item.tipoItem || '').toLowerCase().trim();
      return itemType === 'order_bump' || itemType === 'orderbump';
    }

    function computeOrderBumpMetrics(carrinho) {
      const items = Array.isArray(carrinho) ? carrinho : [];
      let itens = 0;
      let valor = 0;
      for (const item of items) {
        if (!isOrderBumpItem(item)) continue;
        const qtd = Math.max(1, parseInt(item.quantidade ?? item.qty, 10) || 1);
        const preco = toNumber(item.preco ?? item.precoUnitario);
        itens += qtd;
        valor += preco * qtd;
      }
      return {
        itens,
        valor: Number(valor.toFixed(2))
      };
    }

    function carregarFretes() {
      const container = document.getElementById('frete-cards-container');
      const section = document.getElementById('frete-section');
      if (!container) return;
      if (section) section.style.display = 'block';
      container.innerHTML = '<div style="display:flex;align-items:center;gap:8px;padding:12px 0;color:#9ca3af;font-size:13px"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/></svg>Carregando fretes…</div>';

      fetch('frete.json', { cache: 'no-store' })
        .then((res) => res.json())
        .then((data) => {
          state.fretes = Array.isArray(data)
            ? data.map((frete, index) => ({
              ...frete,
              id: frete.id !== undefined ? frete.id : index,
              preco: toNumber(frete.preco)
            }))
            : [];
          if (!state.fretes.length) {
            container.innerHTML = '<p style="font-size:13px;color:#9ca3af;padding:8px 0">Nenhuma opção de frete disponível.</p>';
            return;
          }

          const savedId = localStorage.getItem('checkout_frete_id');
          container.innerHTML = '';
          state.fretes.forEach((frete, index) => {
            const checked = savedId ? String(savedId) === String(frete.id) : index === 0;
            const card = document.createElement('label');
            card.style.cssText = 'display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px 14px;border-radius:12px;cursor:pointer;border:2px solid ' + (checked ? '#fe2c55' : '#e5e7eb') + ';background:' + (checked ? '#fff5f7' : '#fff') + ';transition:border-color .15s,background .15s';
            card.innerHTML = `
              <div style="display:flex;align-items:center;gap:12px;flex:1;min-width:0">
                <input type="radio" name="frete" value="${frete.id}" data-preco="${frete.preco}" data-titulo="${frete.titulo || 'Frete'}" ${checked ? 'checked' : ''} style="accent-color:#fe2c55;width:16px;height:16px;flex-shrink:0">
                <div style="min-width:0">
                  <p style="font-size:13px;font-weight:700;color:#111;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${frete.titulo || 'Frete'}</p>
                  ${frete.descricao ? `<p style="font-size:11px;color:#9ca3af;margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${frete.descricao}</p>` : ''}
                </div>
              </div>
              <span style="font-size:14px;font-weight:700;color:#111;flex-shrink:0">${formatBRL(frete.preco)}</span>
            `;
            const input = card.querySelector('input');
            input.addEventListener('change', () => {
              container.querySelectorAll('label').forEach(l => {
                l.style.borderColor = '#e5e7eb';
                l.style.background = '#fff';
              });
              card.style.borderColor = '#fe2c55';
              card.style.background = '#fff5f7';
              aplicarFrete(input);
            });
            container.appendChild(card);
            if (checked) { aplicarFrete(input); }
          });
        })
        .catch((error) => {
          console.error('Erro ao carregar fretes:', error);
          container.innerHTML = '<p style="font-size:13px;color:#fe2c55;padding:8px 0">Não foi possível carregar as opções de frete.</p>';
        });
    }

    function aplicarFrete(input) {
      if (!input) return;
      state.selectedFrete = {
        id: input.value,
        titulo: input.dataset.titulo || 'Frete',
        preco: toNumber(input.dataset.preco)
      };
      localStorage.setItem('checkout_frete_id', state.selectedFrete.id);
      const freteSelecionadoEl = document.getElementById('freteSelecionado');
      if (freteSelecionadoEl) {
        freteSelecionadoEl.textContent = `${state.selectedFrete.titulo} (${formatBRL(state.selectedFrete.preco)})`;
      }
      updateTotals();
    }

    function coletarDadosCheckout() {
      return {
        comprador: {
          email: getInputValue('email'),
          telefone: getInputValue('telefone'),
          nome: getInputValue('nome'),
          cpf: getInputValue('cpf')
        },
        entrega: {
          cep: getInputValue('cep'),
          endereco: getInputValue('endereco'),
          numero: getInputValue('numero'),
          bairro: getInputValue('bairro'),
          cidade: getInputValue('cidade'),
          estado: getInputValue('estado'),
          complemento: getInputValue('complemento')
        }
      };
    }

    /* ── Máscaras ─────────────────────────────────────────── */
    function maskTelefone(v) {
      v = v.replace(/\D/g, '').slice(0, 11);
      if (v.length <= 10) return v.replace(/(\d{2})(\d{4})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '');
      return v.replace(/(\d{2})(\d{5})(\d{0,4})/, '($1) $2-$3').replace(/-$/, '');
    }
    function maskCpfCnpj(v) {
      v = v.replace(/\D/g, '');
      if (v.length <= 11) return v.replace(/(\d{3})(\d{3})(\d{3})(\d{0,2})/, '$1.$2.$3-$4').replace(/-$/, '').replace(/\.$/g, '');
      v = v.slice(0, 14);
      return v.replace(/(\d{2})(\d{3})(\d{3})(\d{4})(\d{0,2})/, '$1.$2.$3/$4-$5').replace(/-$/, '').replace(/\/$/, '');
    }
    function maskCep(v) {
      v = v.replace(/\D/g, '').slice(0, 8);
      return v.replace(/(\d{5})(\d{0,3})/, '$1-$2').replace(/-$/, '');
    }

    /* ── Algoritmos CPF/CNPJ ────────────────────────────────── */
    function validarCPF(s) {
      const d = s.replace(/\D/g, '');
      if (d.length !== 11 || /^(\d)\1{10}$/.test(d)) return false;
      let s1 = 0; for (let i = 0; i < 9; i++) s1 += +d[i] * (10 - i);
      let r1 = (s1 * 10) % 11; if (r1 >= 10) r1 = 0;
      if (r1 !== +d[9]) return false;
      let s2 = 0; for (let i = 0; i < 10; i++) s2 += +d[i] * (11 - i);
      let r2 = (s2 * 10) % 11; if (r2 >= 10) r2 = 0;
      return r2 === +d[10];
    }
    function validarCNPJ(s) {
      const d = s.replace(/\D/g, '');
      if (d.length !== 14 || /^(\d)\1{13}$/.test(d)) return false;
      const calc = (len) => {
        let s = 0, pos = len - 7;
        for (let i = len; i >= 1; i--) { s += +d[len - i] * pos--; if (pos < 2) pos = 9; }
        const r = s % 11; return r < 2 ? 0 : 11 - r;
      };
      return calc(12) === +d[12] && calc(13) === +d[13];
    }

    /* ── Helpers de erro ────────────────────────────────────── */
    function setFieldError(id, msg) {
      const input = document.getElementById(id);
      const err = document.getElementById(id + '-error');
      if (input) { input.style.borderBottomColor = '#fe2c55'; input.style.backgroundColor = '#fff5f7'; }
      if (err) { err.textContent = msg; err.style.display = 'block'; }
    }
    function clearFieldError(id) {
      const input = document.getElementById(id);
      const err = document.getElementById(id + '-error');
      if (input) { input.style.borderBottomColor = ''; input.style.backgroundColor = ''; }
      if (err) { err.style.display = 'none'; err.textContent = ''; }
    }

    /* ── Validação real por campo ────────────────────────────── */
    function validarCampo(id) {
      const input = document.getElementById(id);
      if (!input) return true;
      const v = input.value.trim();
      clearFieldError(id);
      if (id === 'email') {
        if (!v) { setFieldError(id, t('err_email_required')); return false; }
        if (!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(v)) { setFieldError(id, t('err_email_invalid')); return false; }
      } else if (id === 'telefone') {
        const nums = v.replace(/\D/g, '');
        if (!nums) { setFieldError(id, t('err_phone_required')); return false; }
        if (nums.length < 10 || nums.length > 11) { setFieldError(id, t('err_phone_incomplete')); return false; }
        if (+nums.slice(0, 2) < 11 || +nums.slice(0, 2) > 99) { setFieldError(id, t('err_ddd_invalid')); return false; }
      } else if (id === 'nome') {
        if (!v) { setFieldError(id, t('err_name_required')); return false; }
        const partes = v.split(/\s+/).filter(p => p.length >= 2);
        if (partes.length < 2) { setFieldError(id, t('err_name_surname')); return false; }
      } else if (id === 'cpf') {
        const nums = v.replace(/\D/g, '');
        if (!nums) { setFieldError(id, t('err_cpf_required')); return false; }
        if (nums.length <= 11) {
          if (!validarCPF(nums)) { setFieldError(id, t('err_cpf_invalid')); return false; }
        } else {
          if (!validarCNPJ(nums)) { setFieldError(id, t('err_cnpj_invalid')); return false; }
        }
      } else if (id === 'cep') {
        const nums = v.replace(/\D/g, '');
        if (nums.length !== 8) { setFieldError(id, t('err_zip_digits')); return false; }
      } else if (id === 'numero') {
        if (!v) { setFieldError(id, t('err_number_required')); return false; }
      } else {
        if (!v) { const label = document.querySelector(`label[for="${id}"]`); setFieldError(id, `Informe ${label ? label.textContent.toLowerCase() : 'o campo'}.`); return false; }
      }
      return true;
    }

    function validarEtapa(campos) {
      let ok = true;
      let primeiroErro = null;
      for (const id of campos) {
        if (!validarCampo(id)) {
          ok = false;
          if (!primeiroErro) primeiroErro = document.getElementById(id);
        }
      }
      if (primeiroErro) primeiroErro.focus();
      return ok;
    }

    function proximaEtapa(etapa) {
      if (etapa === 2 && !validarEtapa(['email', 'telefone', 'nome', 'cpf'])) return;
      if (etapa === 3 && !validarEtapa(['cep', 'endereco', 'numero', 'bairro', 'cidade', 'estado'])) return;

      document.querySelectorAll('.step').forEach((step, index) => {
        step.classList.toggle('hidden', index + 1 !== etapa);
      });
      atualizarSteps(etapa);
      currentStep = etapa;
      updateOrderBumpVisibility();
    }

    function atualizarSteps(etapa) {
      for (let i = 1; i <= 3; i += 1) {
        const indicator = document.getElementById(`step-${i}-indicator`);
        if (!indicator) continue;
        indicator.className = 'w-8 h-8 flex items-center justify-center rounded-full text-sm font-semibold';
        if (i < etapa) {
          indicator.classList.add('bg-emerald-500', 'text-white');
        } else if (i === etapa) {
          indicator.classList.add('bg-slate-900', 'text-white');
        } else {
          indicator.classList.add('bg-slate-200', 'text-slate-400');
        }
      }
    }

    function removerDoCarrinho(index) {
      if (!Array.isArray(state.carrinho)) return;
      const idx = Number.parseInt(index, 10);
      if (Number.isNaN(idx) || idx < 0 || idx >= state.carrinho.length) return;
      state.carrinho.splice(idx, 1);
      persistCarrinho();
      renderCarrinho();
      // Reabilita botões de order bump se o item removido for um deles
      refreshOrderBumpButtonsFromCart();
    }

    function atualizarQuantidade(index, novaQuantidade) {
      if (!Array.isArray(state.carrinho)) return;
      const idx = Number.parseInt(index, 10);
      if (Number.isNaN(idx) || idx < 0 || idx >= state.carrinho.length) return;
      const qty = Math.max(0, Number.parseInt(novaQuantidade, 10) || 0);
      if (qty <= 0) {
        state.carrinho.splice(idx, 1);
      } else {
        state.carrinho[idx].quantidade = qty;
        state.carrinho[idx].qty = qty;
      }
      persistCarrinho();
      renderCarrinho();
      refreshOrderBumpButtonsFromCart();
    }

    async function finalizarCompra() {
      const btnComprar = document.getElementById('button-comprar'); // ✅ captura o botão

      // 🔒 Desativa o botão para impedir múltiplos cliques
      if (btnComprar) {
        btnComprar.disabled = true;
        btnComprar.textContent = 'Processando...';
      }

      // Validate all fields before submitting
      if (!validarEtapa(['email', 'telefone', 'nome', 'cpf', 'cep', 'endereco', 'numero', 'bairro', 'cidade', 'estado'])) {
        if (btnComprar) { btnComprar.disabled = false; btnComprar.textContent = 'Fazer pedido'; btnComprar.style.background = '#fe2c55'; }
        abrirTelaEndereco();
        return;
      }

      if (!state.carrinho.length) {
        showToast('Seu carrinho está vazio');
        return;
      }
      if (!state.selectedFrete) {
        showToast(t('select_shipping_first'));
        return;
      }

      const total = updateTotals();
      if (!Number.isFinite(total) || total <= 0) {
        showToast(t('total_calc_error'));
        return;
      }

      const dados = coletarDadosCheckout();
      const subtotal = Number.isFinite(state.subtotal) ? Number(state.subtotal.toFixed(2)) : 0;
      const descontoProduto = Number.isFinite(state.desconto) ? Number(state.desconto.toFixed(2)) : 0;
      const descontoCupom = Number.isFinite(state.cupomDesconto) ? Number(state.cupomDesconto.toFixed(2)) : 0;
      const desconto = Number((descontoProduto + descontoCupom).toFixed(2));

      const orderBumpMetrics = computeOrderBumpMetrics(state.carrinho);
      const ordem = {
        carrinho: state.carrinho,
        subtotal,
        desconto,
        frete: state.selectedFrete,
        total,
        metricas: {
          itens_total: state.carrinho.reduce((acc, item) => acc + Math.max(1, parseInt(item.quantidade ?? item.qty, 10) || 1), 0),
          itens_order_bump: orderBumpMetrics.itens,
          valor_order_bump: orderBumpMetrics.valor,
          valor_sem_order_bump: Number((Math.max(0, total - orderBumpMetrics.valor)).toFixed(2)),
        },
        comprador: dados.comprador,
        entrega: dados.entrega,
        criadoEm: new Date().toISOString()
      };
      sessionStorage.setItem('checkoutOrdem', JSON.stringify(ordem));
      persistCarrinho();

      try {
        const response = await fetch('pix_teste.php', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            valor: total,
            comprador: dados.comprador,
            entrega: dados.entrega,
            carrinho: state.carrinho,
            frete: state.selectedFrete,
            shipping: state.selectedFrete ? { fee: state.selectedFrete.preco } : undefined,
            subtotal,
            desconto,
            metricas: ordem.metricas
          })
        });
        const raw = await response.text();
        let data;
        try {
          data = raw ? JSON.parse(raw) : null;
        } catch (parseError) {
          console.error('Resposta Pix inválida:', raw);
          throw new Error('Resposta inválida do provedor Pix.');
        }
        if (!response.ok || !data || data.success === false) {
          throw new Error((data && data.message) || raw || 'Falha ao gerar cobrança Pix.');
        }

        let qrCode = null;
        if (data && typeof data === 'object') {
          qrCode = data.qr_code || data.pix_qr_code || data.pixCode || data.copy_and_paste || null;
          if (!qrCode && data.pix && typeof data.pix === 'object') {
            qrCode =
              data.pix.qr_code ||
              data.pix.pix_qr_code ||
              data.pix.copy_and_paste ||
              data.pix.copiaecola ||
              null;
          }
        }

        const pixImage =
          data?.qr_code_base64 ??
          data?.pix_qr_code_base64 ??
          data?.pix?.qr_code_base64 ??
          data?.pix?.qrCodeImage ??
          data?.pix?.qr_code_image ??
          null;

        const referenceId = data?.referenceId || data?.externalRef || data?.data?.referenceId || data?.data?.externalRef || data?.items?.[0]?.externalRef || null;
        const transactionId = data?.transactionId || data?.id || data?.data?.id || null;
        const conversionRef = String(referenceId || transactionId || '').trim();
        const conversionEventId = conversionRef !== '' ? `ttk_order_${conversionRef}` : `ttk_order_${Date.now()}`;

        const parseGatewayDiverted = (value) => {
          if (value === true || value === false) return value;
          if (value === 1 || value === 0) return value === 1;
          if (typeof value !== 'string') return null;
          const normalized = value.trim().toLowerCase();
          if (['1', 'true', 'sim', 'yes'].includes(normalized)) return true;
          if (['0', 'false', 'nao', 'no'].includes(normalized)) return false;
          return null;
        };

        const resolveGatewayDiverted = (payload) => {
          if (!payload || typeof payload !== 'object') return null;
          const candidates = [
            payload.gateway_diverted,
            payload.gatewayDiverted,
            payload.tracking_context?.gateway_diverted,
            payload.trackingContext?.gateway_diverted,
            payload.trackingParameters?.gateway_diverted,
            payload.data?.tracking_context?.gateway_diverted,
            payload.data?.trackingParameters?.gateway_diverted,
            payload.pix?.tracking_context?.gateway_diverted,
            payload.pix?.trackingParameters?.gateway_diverted
          ];
          for (const candidate of candidates) {
            const parsed = parseGatewayDiverted(candidate);
            if (parsed !== null) return parsed;
          }
          return null;
        };

        const gatewayDiverted = resolveGatewayDiverted(data) === true;
        window.RABBITFY_GATEWAY_DIVERTED = gatewayDiverted === true;
        if (gatewayDiverted === false) {
          if (typeof window.maybeLoadTikTok === 'function') {
            window.maybeLoadTikTok();
          }
        }

        const ordemAtualizada = {
          ...ordem,
          pix: data,
          pixCode: qrCode,
          pixImage,
          referenceId: referenceId || undefined,
          transactionId: transactionId || undefined,
          gatewayDiverted
        };
        sessionStorage.setItem('checkoutOrdem', JSON.stringify(ordemAtualizada));

        if (typeof window.ttqFire === 'function') {
          const generatedPayload = {
            value: Number(total || 0),
            currency: 'BRL',
            quantity: Number(ordem.metricas?.itens_total || 0),
            order_bump_value: Number(ordem.metricas?.valor_order_bump || 0),
            order_bump_items: Number(ordem.metricas?.itens_order_bump || 0),
            event_stage: 'pedido_gerado',
            event_id: conversionEventId
          };

          if (!gatewayDiverted) {
            window.ttqFire('InitiateCheckout', generatedPayload);
            if (window.zgTrack) window.zgTrack('InitiateCheckout', generatedPayload.value || 0);
          }

          if (!gatewayDiverted && window.TIKTOK_MARK_AS_PAID === true) {
            const generatedPaidMarkerKey = conversionRef !== '' ? `ttk_generated_paid_${conversionRef}` : '';
            const paidTrackedLockKey = conversionRef !== '' ? `ttk_paid_tracked_${conversionRef}` : '';
            if (generatedPaidMarkerKey !== '') {
              try {
                sessionStorage.setItem(generatedPaidMarkerKey, '1');
                localStorage.setItem(generatedPaidMarkerKey, '1');
              } catch (error) {
              }
            }
            if (paidTrackedLockKey !== '') {
              try {
                sessionStorage.setItem(paidTrackedLockKey, '1');
                localStorage.setItem(paidTrackedLockKey, '1');
              } catch (error) {
              }
            }

            window.ttqFire('CompletePayment', {
              ...generatedPayload,
              generated_as_paid: true
            });
            window.ttqFire('Purchase', {
              ...generatedPayload,
              generated_as_paid: true
            });
            console.log('🔥 TikTok Pixel - Pedido gerado marcado como pago');
          }
        }

        const destinoQs = new URLSearchParams();
        if (qrCode) destinoQs.set('qr', qrCode);
        if (referenceId) destinoQs.set('ref', String(referenceId));
        if (transactionId) destinoQs.set('tx', String(transactionId));
        const destino = destinoQs.toString() ? `payment.php?${destinoQs.toString()}` : 'payment.php';
        window.location.href = destino;
      } catch (error) {
        console.error('Erro ao gerar Pix:', error);
        showToast(t('pix_error'));
      }
    }

    function preencherFormulario() {
      try {
        const armazenado = sessionStorage.getItem('checkoutOrdem');
        if (!armazenado) return;
        const ordem = JSON.parse(armazenado);
        if (!ordem || typeof ordem !== 'object') return;
        state.ordemAnterior = ordem;

        if (Array.isArray(ordem.carrinho) && ordem.carrinho.length) {
          state.carrinho = ordem.carrinho.map(normalizarItemCarrinho);
        }
        if (ordem.frete) {
          state.selectedFrete = {
            id: ordem.frete.id,
            titulo: ordem.frete.titulo,
            preco: toNumber(ordem.frete.preco)
          };
          if (ordem.frete.id !== undefined) {
            localStorage.setItem('checkout_frete_id', ordem.frete.id);
          }
        }

        if (ordem.comprador) {
          const emailEl = document.getElementById('email');
          if (emailEl) emailEl.value = ordem.comprador.email || '';
          const telefoneEl = document.getElementById('telefone');
          if (telefoneEl) telefoneEl.value = ordem.comprador.telefone || '';
          const nomeEl = document.getElementById('nome');
          if (nomeEl) nomeEl.value = ordem.comprador.nome || '';
          const cpfEl = document.getElementById('cpf');
          if (cpfEl) cpfEl.value = ordem.comprador.cpf || '';
        }

        if (ordem.entrega) {
          const cepEl = document.getElementById('cep');
          if (cepEl) cepEl.value = ordem.entrega.cep || '';
          const enderecoEl = document.getElementById('endereco');
          if (enderecoEl) enderecoEl.value = ordem.entrega.endereco || '';
          const numeroEl = document.getElementById('numero');
          if (numeroEl) numeroEl.value = ordem.entrega.numero || '';
          const bairroEl = document.getElementById('bairro');
          if (bairroEl) bairroEl.value = ordem.entrega.bairro || '';
          const cidadeEl = document.getElementById('cidade');
          if (cidadeEl) cidadeEl.value = ordem.entrega.cidade || '';
          const estadoEl = document.getElementById('estado');
          if (estadoEl) estadoEl.value = ordem.entrega.estado || '';
          const complementoEl = document.getElementById('complemento');
          if (complementoEl) complementoEl.value = ordem.entrega.complemento || '';
        }
      } catch (error) {
        console.warn('Não foi possível preencher dados anteriores:', error);
      }
    }

    function iniciarContador() {
      let segundos = 5 * 3600;
      const span = document.getElementById('contador');
      if (!span) return;
      const tick = () => {
        if (segundos <= 0) {
          span.textContent = 'O cupom expirou';
          return;
        }
        const horas = String(Math.floor(segundos / 3600)).padStart(2, '0');
        const minutos = String(Math.floor((segundos % 3600) / 60)).padStart(2, '0');
        const secs = String(segundos % 60).padStart(2, '0');
        span.textContent = `O cupom expira em ${horas}:${minutos}:${secs}`;
        segundos -= 1;
        setTimeout(tick, 1000);
      };
      tick();
    }

    function abrirModalCartaoIndisponivel() {
      const overlay = document.getElementById('modal-cartao-overlay');
      const modal = document.getElementById('modal-cartao');
      if (!overlay || !modal) return;
      overlay.style.display = 'block';
      modal.style.display = 'block';
      requestAnimationFrame(() => { modal.style.transform = 'translateY(0)'; });
    }
    function fecharModalCartao() {
      const overlay = document.getElementById('modal-cartao-overlay');
      const modal = document.getElementById('modal-cartao');
      if (!modal) return;
      modal.style.transform = 'translateY(100%)';
      setTimeout(() => {
        if (overlay) overlay.style.display = 'none';
        modal.style.display = 'none';
      }, 300);
    }

    function updateCpfLabel(val) {
      const digits = val.replace(/\D/g, '');
      const lbl = document.getElementById('cpf-label');
      if (!lbl) return;
      lbl.textContent = digits.length > 11 ? 'CNPJ' : 'CPF';
    }

    function updateAddrSummary() {
      const nome = (document.getElementById('nome')?.value || '').trim();
      const tel = (document.getElementById('telefone')?.value || '').trim();
      const end = (document.getElementById('endereco')?.value || '').trim();
      const num = (document.getElementById('numero')?.value || '').trim();
      const bairro = (document.getElementById('bairro')?.value || '').trim();
      const cidade = (document.getElementById('cidade')?.value || '').trim();
      const emptyRow = document.getElementById('addr-empty-row');
      const filledRow = document.getElementById('addr-filled-row');
      if (!emptyRow || !filledRow) return;
      if (nome && tel && end) {
        const firstName = nome.split(' ')[0];
        const digits = tel.replace(/\D/g,'');
        const maskedTel = digits.length > 4 ? '(' + digits.slice(0,2) + ') ' + digits.slice(2,-4).replace(/\d/g,'*') + digits.slice(-4) : tel;
        const nameEl = document.getElementById('addr-summary-name');
        const addrEl = document.getElementById('addr-summary-addr');
        if (nameEl) nameEl.textContent = firstName + ' • ' + maskedTel;
        if (addrEl) addrEl.textContent = end + (num ? ', ' + num : '') + (bairro ? ' - ' + bairro : '') + (cidade ? ', ' + cidade : '');
        emptyRow.style.display = 'none';
        filledRow.style.display = 'flex';
      } else {
        emptyRow.style.display = 'flex';
        filledRow.style.display = 'none';
      }
    }

    function _showTtkLoader(cb) {
      const l = document.getElementById('ttk-loader');
      if (!l) { cb && cb(); return; }
      l.classList.remove('hiding');
      l.style.opacity = '1';
      l.style.pointerEvents = 'all';
      setTimeout(() => { l.classList.add('hiding'); l.style.pointerEvents = 'none'; setTimeout(() => { l.style.opacity = '0'; }, 260); cb && cb(); }, 420);
    }

    function abrirTelaEndereco() {
      const tela = document.getElementById('tela-endereco');
      if (!tela) return;
      _showTtkLoader(() => {
        tela.style.display = 'flex';
        document.body.style.overflow = 'hidden';
        requestAnimationFrame(() => { tela.style.transform = 'translateX(0)'; });
      });
    }

    function fecharTelaEndereco() {
      const tela = document.getElementById('tela-endereco');
      if (!tela) return;
      tela.style.transform = 'translateX(100%)';
      document.body.style.overflow = '';
      setTimeout(() => { tela.style.display = 'none'; }, 300);
    }

    function salvarEndereco() {
      const campos = ['nome', 'telefone', 'email', 'cep', 'endereco', 'numero', 'bairro', 'cidade', 'estado'];
      if (!validarEtapa(campos)) return;
      updateAddrSummary();
      fecharTelaEndereco();
      carregarFretes();
    }

    let _resumoOpen = true;
    function toggleResumoFinanceiro() {
      _resumoOpen = !_resumoOpen;
      const detail = document.getElementById('resumo-detail');
      const chevron = document.getElementById('resumo-chevron');
      if (detail) detail.style.display = _resumoOpen ? 'block' : 'none';
      if (chevron) chevron.style.transform = _resumoOpen ? '' : 'rotate(180deg)';
    }

    document.addEventListener('DOMContentLoaded', async () => {
      // Esconder loader de página
      (function() {
        const l = document.getElementById('ttk-loader');
        if (!l) return;
        l.classList.add('hiding');
        setTimeout(() => { l.style.display = 'none'; }, 300);
      })();
      loadCarrinho();
      if (!state.carrinho.length) {
        await carregarCarrinhoDaQuery();
      }
      window.addEventListener('cupons-atualizados', function () {
        updateTotals();
      });
      await carregarOrderBump();
      preencherFormulario();
      renderCarrinho();
      updateAddrSummary();
      carregarFretes();
      iniciarContador();
      updateOrderBumpVisibility();

      // Wire CEP auto lookup
      const cepInput = document.getElementById('cep');
      if (cepInput) {
        cepInput.addEventListener('input', onCepInput);
        cepInput.addEventListener('blur', tryLookupCep);
      }
    });

    function formatCEP(v) {
      const digits = String(v || '').replace(/\D/g, '').slice(0, 8);
      if (digits.length <= 5) return digits;
      return `${digits.slice(0, 5)}-${digits.slice(5)}`;
    }

    function onCepInput(e) {
      const el = e.target;
      const cur = el.value;
      const formatted = formatCEP(cur);
      if (cur !== formatted) {
        const pos = el.selectionStart;
        el.value = formatted;
        // tenta manter o cursor próximo ao fim
        try { el.setSelectionRange(formatted.length, formatted.length); } catch {}
      }
      // debounce lookup when 8 digits
      if (cepLookupTimer) clearTimeout(cepLookupTimer);
      cepLookupTimer = setTimeout(() => tryLookupCep(), 400);
    }

    async function tryLookupCep() {
      const cepEl = document.getElementById('cep');
      const msgEl = document.getElementById('cep-msg');
      if (!cepEl) return;
      const digits = cepEl.value.replace(/\D/g, '');
      if (digits.length !== 8) {
        if (msgEl) { msgEl.textContent = ''; msgEl.classList.add('hidden'); }
        return;
      }
      if (digits === lastCepLookedUp) return; // evita chamada repetida
      lastCepLookedUp = digits;
      if (msgEl) { msgEl.textContent = 'Buscando CEP...'; msgEl.classList.remove('hidden'); msgEl.classList.remove('text-rose-600'); msgEl.classList.add('text-slate-600'); }
      try {
        const resp = await fetch(`https://viacep.com.br/ws/${digits}/json/`, { cache: 'no-store' });
        const data = await resp.json();
        if (!resp.ok || data.erro) {
          throw new Error('CEP não encontrado');
        }
        // Preenche campos
        const enderecoEl = document.getElementById('endereco');
        const bairroEl = document.getElementById('bairro');
        const cidadeEl = document.getElementById('cidade');
        const estadoEl = document.getElementById('estado');
        if (enderecoEl) enderecoEl.value = (data.logradouro || '').trim();
        if (bairroEl) bairroEl.value = (data.bairro || '').trim();
        if (cidadeEl) cidadeEl.value = (data.localidade || '').trim();
        if (estadoEl) estadoEl.value = (data.uf || '').trim().toUpperCase();

        const numeroEl = document.getElementById('numero');
        if (numeroEl && !numeroEl.value) numeroEl.focus();

        if (msgEl) { msgEl.textContent = t('zip_autofilled'); msgEl.classList.remove('text-rose-600'); msgEl.classList.add('text-emerald-600'); msgEl.classList.remove('hidden'); }
      } catch (err) {
        if (msgEl) { msgEl.textContent = t('err_zip_invalid'); msgEl.classList.remove('hidden'); msgEl.classList.remove('text-slate-600'); msgEl.classList.add('text-rose-600'); }
      }
    }

    async function carregarOrderBump() {
      try {
        const resp = await fetch('produtos.json', { cache: 'no-store' });
        if (!resp.ok) return;
        const arr = await resp.json();
        const produtos = Array.isArray(arr) ? arr : [];
        if (!produtos.length) return;
        const produtosPorId = new Map(produtos.filter(p => p && p.id !== undefined && p.id !== null).map(p => [String(p.id), p]));

        const isTruthy = (value) => {
          if (typeof value === 'boolean') return value;
          if (typeof value === 'number') return value === 1;
          const normalized = String(value ?? '').toLowerCase().trim();
          return ['1', 'true', 'yes', 'on', 'ativo', 'enabled'].includes(normalized);
        };

        const normalizeOrderBumpProduct = (source) => {
          if (!source || typeof source !== 'object') return null;
          const idRaw = source.id ?? source.produto_id ?? source.product_id ?? null;
          if (idRaw === null || idRaw === undefined || String(idRaw).trim() === '') return null;

          const primaryMedia = source.imagem || source.image || source.foto || source.thumb || source.thumbnail || '';
          const fotos = Array.isArray(source.fotos)
            ? source.fotos.map(stripOrderBumpMediaHost).filter(Boolean)
            : primaryMedia
              ? [stripOrderBumpMediaHost(primaryMedia)]
              : [];

          return {
            id: idRaw,
            titulo: source.titulo || source.nome || source.name || 'Item adicional',
            preco: toNumber(source.preco ?? source.price ?? 0),
            preco_comparacao: toNumber(source.preco_comparacao ?? source.precoComparacao ?? source.compare_at_price ?? 0),
            fotos,
            variacoes: Array.isArray(source.variacoes) ? source.variacoes : [],
            full_slug: source.full_slug || source.slug_path || source.slug || source.path || '',
          };
        };

        // 1) Tenta pegar pelo parâmetro da URL
        const params = new URLSearchParams(window.location.search);
        const urlProdutoId = params.get('produto_id') || params.get('produto');
        let produto = null;
        if (urlProdutoId) {
          produto = produtos.find(p => String(p.id) === String(urlProdutoId)) || null;
        }

        // 2) Tenta pelo produtoId do carrinho
        if (!produto && state.carrinho && state.carrinho.length) {
          const idCarrinho = state.carrinho[0]?.produtoId || state.carrinho[0]?.id || null;
          if (idCarrinho !== null && idCarrinho !== undefined) {
            produto = produtos.find(p => String(p.id) === String(idCarrinho)) || null;
          }
        }

        // 3) Fallback por título/variação (ex: título vira "Preto")
        if (!produto && state.carrinho && state.carrinho.length) {
          const tituloCarrinho = (state.carrinho[0]?.titulo || '').toString().trim().toLowerCase();
          if (tituloCarrinho) {
            produto = produtos.find(p => {
              const base = (p.titulo || '').toString().trim().toLowerCase();
              if (base && base === tituloCarrinho) return true;
              const vars = Array.isArray(p.variacoes) ? p.variacoes : [];
              return vars.some(v => (v.titulo || '').toString().trim().toLowerCase() === tituloCarrinho);
            }) || null;
          }
        }

        // 4) Último fallback: primeiro com order_bumps/order_bump/legado; senão, primeiro produto
        if (!produto) {
          produto = produtos.find(p => (Array.isArray(p.order_bumps) && p.order_bumps.length) || !!p.order_bump || (isTruthy(p.order_bump_ativo) && p.order_bump_produto_id)) || produtos[0];
        }

        const obs = [];
        const obsSeen = new Set();
        const pushObs = (entry) => {
          const normalized = normalizeOrderBumpProduct(entry);
          if (!normalized) return;
          const key = String(normalized.id);
          if (obsSeen.has(key)) return;
          obsSeen.add(key);
          obs.push(normalized);
        };
        if (produto) {
          if (Array.isArray(produto.order_bumps) && produto.order_bumps.length) {
            produto.order_bumps.forEach(pushObs);
          }

          if (produto.order_bump) {
            pushObs(produto.order_bump);
          }

          if (isTruthy(produto.order_bump_ativo) && produto.order_bump_produto_id) {
            const idList = Array.isArray(produto.order_bump_produto_id)
              ? produto.order_bump_produto_id
              : String(produto.order_bump_produto_id).split(',').map(id => id.trim()).filter(Boolean);
            idList.forEach((id) => {
              const target = produtosPorId.get(String(id));
              if (target) {
                pushObs(target);
              }
            });
          }
        }
        if (!obs.length) return;
        renderOrderBumpCards(obs);
      } catch (e) {
        console.warn('Order bump não disponível:', e);
      }
    }

    function isOrderBumpInCart(obId) {
      return state.carrinho.some(i => isOrderBumpItem(i) && String(i.produtoId) === String(obId));
    }

    function getOrderBumpCartQty(obId) {
      return state.carrinho.reduce((acc, item) => {
        if (!isOrderBumpItem(item)) return acc;
        if (String(item.produtoId) !== String(obId)) return acc;
        const qtd = Math.max(1, parseInt(item.quantidade ?? item.qty, 10) || 1);
        return acc + qtd;
      }, 0);
    }

    function getOrderBumpDesiredQty(obId) {
      const current = parseInt(window.__orderBumpQty.get(String(obId)), 10);
      if (Number.isFinite(current) && current > 0) return current;
      return 1;
    }

    function setOrderBumpDesiredQty(obId, qty) {
      const safeQty = Math.max(1, parseInt(qty, 10) || 1);
      window.__orderBumpQty.set(String(obId), safeQty);
      return safeQty;
    }

    function extractChancePercent(text) {
      const match = String(text || '').match(/chance\s+de\s*([0-9]+(?:[\.,][0-9]+)?)\s*%/i);
      if (!match) return null;
      const value = toNumber(match[1]);
      return value > 0 ? value : null;
    }

    function updateOrderBumpCardState(obId) {
      const card = document.querySelector(`#order-bumps-container [data-ob-id="${String(obId)}"]`);
      if (!card) return;
      const ob = window.__orderBumps.get(String(obId));
      if (!ob) return;

      const desiredQty = getOrderBumpDesiredQty(obId);
      const cartQty = getOrderBumpCartQty(obId);
      const qtyEl = card.querySelector('[data-role="ob-qty"]');
      const inCartEl = card.querySelector('[data-role="ob-in-cart"]');
      const luckEl = card.querySelector('[data-role="ob-luck"]');
      const cta = card.querySelector('button[data-role="ob-cta"]');

      if (qtyEl) qtyEl.textContent = String(desiredQty);
      if (inCartEl) {
        inCartEl.textContent = cartQty > 0 ? `${cartQty} no carrinho` : 'Nenhum no carrinho';
      }
      if (cta) {
        cta.textContent = desiredQty > 1 ? `Adicionar ${desiredQty} itens` : 'Adicionar item';
      }

      const chanceBase = extractChancePercent(ob.titulo);
      if (luckEl) {
        if (chanceBase) {
          const currentChance = Number((chanceBase * cartQty).toFixed(2));
          const projectedChance = Number((chanceBase * (cartQty + desiredQty)).toFixed(2));
          if (cartQty > 0) {
            luckEl.textContent = `Sorte atual: ${currentChance}% | Após adicionar: ${projectedChance}%`;
          } else {
            luckEl.textContent = `Após adicionar ${desiredQty}x: ${projectedChance}% de sorte`;
          }
          luckEl.classList.remove('hidden');
        } else {
          luckEl.classList.add('hidden');
          luckEl.textContent = '';
        }
      }
    }

    function markOrderBumpRedeemed() {
      // Compatibilidade com chamadas antigas: agora apenas atualiza estado visual.
      refreshOrderBumpButtonsFromCart();
    }

    function refreshOrderBumpButtonsFromCart() {
      const container = document.getElementById('order-bumps-container');
      if (!container) return;
      const cards = container.querySelectorAll('[data-ob-id]');
      cards.forEach((card) => {
        const obId = card.getAttribute('data-ob-id');
        updateOrderBumpCardState(obId);
      });
    }

    function renderOrderBumpCards(lista) {
      const container = document.getElementById('order-bumps-container');
      if (!container) return;
      container.innerHTML = '<h3 class="text-sm font-semibold text-slate-800 text-center">Acho que você vai gostar destas ofertas ;)</h3>';
      for (const ob of lista) {
        // guarda referência global
        window.__orderBumps.set(String(ob.id), ob);
        const card = document.createElement('div');
        card.className = 'bg-transparent';
        card.setAttribute('data-ob-id', String(ob.id));
        const precoAtual = toNumber(ob.preco);
        const precoCmp = toNumber(ob.preco_comparacao ?? ob.precoComparacao ?? 0);
        const descontoValor = precoCmp > precoAtual ? (precoCmp - precoAtual) : 0;
        setOrderBumpDesiredQty(ob.id, getOrderBumpDesiredQty(ob.id));
        card.innerHTML = `
          <div class="border border-dashed border-slate-300 rounded-lg p-3 bg-white">
            <div class="flex gap-3 items-center">
              <img src="${resolveMedia(Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '')}" alt="Order bump" class="w-16 h-16 object-contain rounded border bg-white">
              <div class="flex-1">
                <p class="font-semibold text-sm text-slate-900 leading-snug">${ob.titulo || 'Item adicional'}</p>
                <div class="mt-1">
                  ${precoCmp > precoAtual ? `<p class="text-xs text-slate-400 line-through">${formatBRL(precoCmp)}</p>` : ''}
                  <div class="flex items-baseline gap-2">
                    <span class="text-slate-900 font-bold">${formatBRL(precoAtual)}</span>
                    ${descontoValor > 0 ? `<span class="text-emerald-600 text-xs font-semibold">(${formatBRL(descontoValor)} OFF)</span>` : ''}
                  </div>
                </div>
                <p data-role="ob-luck" class="mt-1 text-[11px] font-semibold text-emerald-700 hidden"></p>
                <div class="mt-2 flex items-center justify-between gap-2">
                  <div class="inline-flex items-center gap-1 bg-slate-100 rounded-lg px-2 py-1">
                    <button type="button" data-role="ob-minus" class="w-6 h-6 flex items-center justify-center text-slate-700">-</button>
                    <span data-role="ob-qty" class="w-6 text-center text-sm font-semibold text-slate-900">1</span>
                    <button type="button" data-role="ob-plus" class="w-6 h-6 flex items-center justify-center text-slate-700">+</button>
                  </div>
                  <span data-role="ob-in-cart" class="text-[11px] text-slate-500">Nenhum no carrinho</span>
                </div>
              </div>
            </div>
          </div>
          <button type="button" data-role="ob-cta" class="w-full mt-2 py-2 text-sm font-semibold rounded-md bg-[#00d8d1] text-white hover:bg-[#00c2bd]">Adicionar item</button>
        `;
        const minusBtn = card.querySelector('button[data-role="ob-minus"]');
        const plusBtn = card.querySelector('button[data-role="ob-plus"]');
        const btn = card.querySelector('button[data-role="ob-cta"]');
        if (minusBtn) {
          minusBtn.addEventListener('click', () => {
            const current = getOrderBumpDesiredQty(ob.id);
            setOrderBumpDesiredQty(ob.id, current - 1);
            updateOrderBumpCardState(ob.id);
          });
        }
        if (plusBtn) {
          plusBtn.addEventListener('click', () => {
            const current = getOrderBumpDesiredQty(ob.id);
            setOrderBumpDesiredQty(ob.id, current + 1);
            updateOrderBumpCardState(ob.id);
          });
        }
        if (btn) {
          btn.addEventListener('click', () => abrirModalOrderBump(ob, getOrderBumpDesiredQty(ob.id)));
        }
        container.appendChild(card);
        updateOrderBumpCardState(ob.id);
      }
      updateOrderBumpVisibility();
      refreshOrderBumpButtonsFromCart();
    }

    function updateOrderBumpVisibility() {
      const container = document.getElementById('order-bumps-container');
      if (!container) return;
      const hasCards = container.children && container.children.length > 0;
      container.style.display = hasCards ? 'flex' : 'none';
      if (hasCards) container.style.flexDirection = 'column';
      if (hasCards) container.style.gap = '12px';
    }

  function abrirModalOrderBump(ob, quantidadeDesejada = 1) {
      // Modal com suporte a dupla seleção (Cor e Tamanho) quando possível
      const overlay = document.createElement('div');
      overlay.className = 'fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4';
      const modal = document.createElement('div');
      modal.className = 'bg-white rounded-lg shadow-xl max-w-md w-full p-4';

      // Monta base de variações
      const variacoesOrig = Array.isArray(ob.variacoes) && ob.variacoes.length ? ob.variacoes : [
        {
          id: null,
          titulo: 'Padrão',
          preco: toNumber(ob.preco),
          preco_comparacao: toNumber(ob.preco_comparacao ?? ob.precoComparacao ?? 0),
          imagem: Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''
        }
      ];

      // Heurística p/ extrair COR e TAMANHO a partir do título/atributos
      const sizeRegex = /^(pp|p|m|g|gg|xg|xs|s|l|xl|xxl|2xl|3xl|4xl|5xl|\d{2}|\d{2}[\/\-]\d{2})$/i;
      const clean = (s) => (s || '').toString().trim().replace(/\s+/g, ' ');
      const getFromObjKeys = (obj, keys) => {
        if (!obj) return null;
        for (const k of keys) {
          if (obj[k] != null && obj[k] !== '') return clean(String(obj[k]));
        }
        return null;
      };
      const getFromArrayAttrs = (arr, keys) => {
        if (!Array.isArray(arr)) return null;
        const lowerKeys = keys.map(k => k.toLowerCase());
        for (const it of arr) {
          const name = clean(it?.name || it?.nome || it?.chave || '');
          const val = it?.value ?? it?.valor ?? it?.val ?? it?.v ?? '';
          if (!name) continue;
          if (lowerKeys.includes(name.toLowerCase())) return clean(String(val));
        }
        return null;
      };
      function parseColorSizeFromTitle(t) {
        const str = clean(t);
        if (!str) return { cor: null, tamanho: null };
        const mCor = str.match(/\bcor\s*[:\-]?\s*([^|\/\-–—×x•+()\[\]]+)/i);
        const mTam = str.match(/\b(tamanho|tam|size)\s*[:\-]?\s*([^|\/\-–—×x•+()\[\]]+)/i);
        let cor = mCor ? clean(mCor[1]) : null;
        let tamanho = mTam ? clean(mTam[2]) : null;
        if (!cor || !tamanho) {
          const parts = str.split(/[|\/\-–—×x•+]+/).map(p => clean(p)).filter(Boolean);
          if (parts.length >= 2) {
            let sizePart = parts.find(p => sizeRegex.test(p));
            if (sizePart) {
              tamanho = tamanho || sizePart;
              const other = parts.find(p => p !== sizePart) || parts[0];
              if (!cor && other && other !== tamanho) cor = other;
            } else if (parts.length >= 2) {
              // Fallback: 1º = cor, 2º = tamanho (se parecer tamanho)
              if (!cor) cor = parts[0];
              if (!tamanho && sizeRegex.test(parts[1])) tamanho = parts[1];
            }
          }
        }
        return { cor: cor || null, tamanho: tamanho || null };
      }

      // Deriva uma cor a partir do título removendo tokens de tamanho e rótulos
      function deriveColorFromTitle(t) {
        const str = clean(t).replace(/\bcor\s*[:\-]?\s*/i, '');
        if (!str) return null;
        const parts = str.split(/[|\/\-–—×x•+]+/).map(p => clean(p)).filter(Boolean);
        const filtered = parts.filter(p => !sizeRegex.test(p));
        return filtered.length ? filtered[0] : null;
      }

      // Normaliza strings para comparação (remove acentos e caixa)
      function normalizeStr(s) {
        return (s || '')
          .toString()
          .normalize('NFD')
          .replace(/[\u0300-\u036f]/g, '')
          .toLowerCase()
          .trim();
      }

      function getVariantImage(variant) {
        if (!variant || typeof variant !== 'object') return '';
        const img = variant.imagem || variant.foto || variant.image || variant.img || '';
        if (img) return img;
        if (Array.isArray(variant.fotos) && variant.fotos.length) return variant.fotos[0];
        return '';
      }

      // Busca a melhor imagem para a cor selecionada
      function findImageForColor(cor, repCandidate = null) {
        const candidate = repCandidate || null;
        const candImg = getVariantImage(candidate);
        if (candImg) return candImg;
        const norm = normalizeStr(cor);
        // Procura em combinações por alguma variação da mesma cor que tenha imagem
        if (Array.isArray(combos) && combos.length) {
          const withImg = combos.find(x => x.cor === cor && x.v && getVariantImage(x.v));
          if (withImg) return getVariantImage(withImg.v);
        }
        // Tenta a partir do mapeamento de cor->variação
        if (colorToVariant && colorToVariant.size) {
          const v = colorToVariant.get(cor);
          const vImg = getVariantImage(v);
          if (vImg) return vImg;
        }
        // Heurística: procurar na lista de fotos por arquivo/url contendo a cor
        if (Array.isArray(ob.fotos) && ob.fotos.length) {
          const match = ob.fotos.find(f => normalizeStr(f).includes(norm));
          if (match) return match;
          // fallback: tenta a primeira foto
          return ob.fotos[0];
        }
        return '';
      }

      // Constrói matriz 2D de combinações
      const combos = [];
      const coresSet = new Set();
      const tamanhosSet = new Set();
      for (const v of variacoesOrig) {
        const titulo = v.titulo || v.info || '';
        // Busca em diferentes estruturas comuns
        const attrsObjList = [v.atributos, v.attributes, v.attr, v.opcoes, v.options, v.especificacoes];
        let cor = null;
        let tamanho = null;
        // Objetos
        for (const maybe of attrsObjList) {
          if (cor && tamanho) break;
          if (maybe && !Array.isArray(maybe) && typeof maybe === 'object') {
            cor = cor || getFromObjKeys(maybe, ['cor', 'Cor', 'color', 'Color']);
            tamanho = tamanho || getFromObjKeys(maybe, ['tamanho', 'Tamanho', 'tam', 'Tam', 'size', 'Size']);
          }
        }
        // Arrays [{name, value}]
        for (const maybe of attrsObjList) {
          if (cor && tamanho) break;
          const gotCor = getFromArrayAttrs(maybe, ['cor', 'color', 'Cor', 'Color']);
          const gotTam = getFromArrayAttrs(maybe, ['tamanho', 'tam', 'size', 'Tamanho', 'Size']);
          if (!cor && gotCor) cor = gotCor;
          if (!tamanho && gotTam) tamanho = gotTam;
        }
        // Título como fallback
        if (!cor || !tamanho) {
          const parsed = parseColorSizeFromTitle(titulo);
          cor = cor || parsed.cor;
          tamanho = tamanho || parsed.tamanho;
        }
        // Usa o campo tipo da variação como pista forte
        const tipoInferido = (v.tipo || '').toString().toLowerCase();
        if (!cor && tipoInferido === 'cor') {
          cor = clean(titulo);
        }
        if (!tamanho && (tipoInferido === 'tamanho' || tipoInferido === 'tam')) {
          tamanho = clean(titulo);
        }
        // Se só um dos dois existir, tenta inferir pelo padrão geral: tokens de tamanho
        if (!tamanho) {
          const parts = clean(titulo).split(/[|\/\-–—×x•+]+/).map(p => clean(p)).filter(Boolean);
          const sizePart = parts.find(p => sizeRegex.test(p));
          if (sizePart) tamanho = sizePart;
        }
        if (cor) coresSet.add(cor);
        if (tamanho) tamanhosSet.add(tamanho);
        if (cor && tamanho) combos.push({ cor, tamanho, v });
      }
  const cores = Array.from(coresSet).filter(c => !sizeRegex.test(String(c || '')));
      const tamanhos = Array.from(tamanhosSet);
      const has2D = cores.length > 0 && tamanhos.length > 0 && combos.length > 0;

      // HTML dinâmico: sempre dois pickers (Cor em cima e Tamanho embaixo)
      modal.innerHTML = `
        <div class="flex items-start gap-3 mb-3">
          <img id="ob-header-thumb" src="${resolveMedia(Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '')}" class="w-16 h-16 object-contain rounded border" alt="${ob.titulo || 'Produto'}">
          <div>
            <h3 class="font-semibold text-slate-900">${ob.titulo || 'Produto'}</h3>
            <div class="text-sm flex items-center gap-2">
              <span id="ob-price" class="font-bold text-emerald-700">${formatBRL(toNumber(ob.preco))}</span>
              ${toNumber(ob.preco_comparacao ?? 0) > toNumber(ob.preco) ? `<span id="ob-price-cmp" class="text-xs text-slate-400 line-through">${formatBRL(toNumber(ob.preco_comparacao))}</span>` : '<span id="ob-price-cmp" class="hidden"></span>'}
            </div>
          </div>
        </div>
        <div class="grid grid-cols-1 gap-3 mb-3">
          <div>
            <label class="block text-sm font-medium text-slate-700 mb-1">Cor</label>
            <div class="relative">
              <button type="button" id="ob-cor-picker" class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm text-left bg-white hover:bg-slate-50 flex items-center gap-3">
                <img id="ob-cor-thumb" class="w-8 h-8 object-cover rounded border hidden" alt="Cor">
                <span id="ob-cor-label" class="text-slate-800">Selecione a cor…</span>
              </button>
              <div id="ob-cor-dropdown" class="absolute z-50 mt-2 w-full bg-white border border-slate-200 rounded-md shadow-lg max-h-64 overflow-auto hidden">
                <ul id="ob-cor-list" class="divide-y divide-slate-100"></ul>
              </div>
            </div>
          </div>
          <div>
            <label class="block text-sm font-medium text-slate-700 mb-1">Tamanho</label>
            <div class="relative">
              <button type="button" id="ob-tam-picker" class="w-full rounded-md border border-slate-300 px-3 py-2 text-sm text-left bg-white hover:bg-slate-50 flex items-center gap-3 disabled:opacity-60" disabled>
                <span id="ob-tam-label" class="text-slate-800">Selecione o tamanho…</span>
              </button>
              <div id="ob-tam-dropdown" class="absolute z-50 mt-2 w-full bg-white border border-slate-200 rounded-md shadow-lg max-h-64 overflow-auto hidden">
                <ul id="ob-tam-list" class="divide-y divide-slate-100"></ul>
              </div>
            </div>
          </div>
        </div>
        <div class="flex justify-end gap-2">
          <button type="button" class="px-4 py-2 rounded-md border" id="ob-cancel">Cancelar</button>
          <button type="button" class="px-4 py-2 rounded-md bg-[#00d8d1] hover:bg-[#00c2bd] text-white font-semibold disabled:opacity-60" id="ob-confirm">Quero esse item</button>
        </div>
      `;

      overlay.appendChild(modal);
      document.body.appendChild(overlay);

  const headerThumb = modal.querySelector('#ob-header-thumb');
  const priceEl = modal.querySelector('#ob-price');
  const priceCmpEl = modal.querySelector('#ob-price-cmp');
  const confirmBtn = modal.querySelector('#ob-confirm');

  let selectedVar = null; // variante final escolhida
  let uiSelectedColor = null; // rótulo de cor escolhido (para exibição)
  let uiSelectedSize = null;  // rótulo de tamanho escolhido (para exibição)
  let selectedColorImageRel = null; // imagem resolvida para a cor escolhida (relativa ou absoluta)

      function applySelectedVariant(v) {
        selectedVar = v ? {
          id: v.id ?? null,
          preco: toNumber(v.preco ?? ob.preco),
          precoCmp: toNumber(v.preco_comparacao ?? v.precoComparacao ?? 0),
          titulo: v.titulo || '',
          imagem: getVariantImage(v) || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '')
        } : null;
        if (selectedVar) {
          const img = resolveMedia(selectedVar.imagem);
          if (headerThumb) headerThumb.src = img;
          if (priceEl) priceEl.textContent = formatBRL(selectedVar.preco);
          if (priceCmpEl) {
            if (selectedVar.precoCmp && selectedVar.precoCmp > selectedVar.preco) {
              priceCmpEl.textContent = formatBRL(selectedVar.precoCmp);
              priceCmpEl.classList.remove('hidden');
            } else {
              priceCmpEl.textContent = '';
              priceCmpEl.classList.add('hidden');
            }
          }
        }
        updateConfirmState();
      }

      function updateConfirmState() {
        // Regras: se existir dimensão de Cor (has2D ou hasColorNow), exigir selectedVar
        // Se for somente tamanho (sem cor), também exigir selectedVar
        const needSelection = (typeof has2D !== 'undefined' && has2D) || (typeof hasColorNow !== 'undefined' && hasColorNow) || (typeof hasSizeNow !== 'undefined' && hasSizeNow);
        if (confirmBtn) {
          confirmBtn.disabled = needSelection && !selectedVar;
        }
      }

      function formatCorTamanhoLabel(cor, tamanho, fallbackTitulo) {
        let c = cor || null;
        let t = tamanho || null;
        if (!c || !t) {
          const parsed = parseColorSizeFromTitle(fallbackTitulo || '');
          c = c || parsed.cor;
          t = t || parsed.tamanho;
        }
        const label = [c, t].filter(Boolean).join(' — ');
        return label || (fallbackTitulo || 'Opção');
      }

      // Referências dos pickers (sempre existem agora)
      const corPicker = modal.querySelector('#ob-cor-picker');
      const corDropdown = modal.querySelector('#ob-cor-dropdown');
      const corList = modal.querySelector('#ob-cor-list');
      const corLabel = modal.querySelector('#ob-cor-label');
      const corThumb = modal.querySelector('#ob-cor-thumb');

      const tamPicker = modal.querySelector('#ob-tam-picker');
      const tamDropdown = modal.querySelector('#ob-tam-dropdown');
      const tamList = modal.querySelector('#ob-tam-list');
      const tamLabel = modal.querySelector('#ob-tam-label');

  const hasColor = cores.length > 0;
  const hasSize = tamanhos.length > 0;

      // Mapeamentos auxiliares para modos parciais
      const colorToVariant = new Map();
      const sizeToVariant = new Map();
  if (has2D) {
        for (const c of cores) {
          const rep = combos.find(x => x.cor === c)?.v;
          if (rep) colorToVariant.set(c, rep);
        }
        for (const t of tamanhos) {
          const rep = combos.find(x => x.tamanho === t)?.v;
          if (rep) sizeToVariant.set(t, rep);
        }
      } else {
        // Preenche a partir das variações reconhecidas
        for (const v of variacoesOrig) {
          const titulo = v.titulo || v.info || '';
          let { cor, tamanho } = parseColorSizeFromTitle(titulo);
          // tenta também nos objetos/arrays
          if (!cor || !tamanho) {
            const attrsObjList = [v.atributos, v.attributes, v.attr, v.opcoes, v.options, v.especificacoes];
            for (const maybe of attrsObjList) {
              if (!cor && maybe && !Array.isArray(maybe) && typeof maybe === 'object') {
                cor = cor || getFromObjKeys(maybe, ['cor', 'Cor', 'color', 'Color']);
              }
              if (!tamanho && maybe && !Array.isArray(maybe) && typeof maybe === 'object') {
                tamanho = tamanho || getFromObjKeys(maybe, ['tamanho', 'Tamanho', 'tam', 'Tam', 'size', 'Size']);
              }
            }
          }
          // evita cadastrar tokens de tamanho como cor
          if (cor && !sizeRegex.test(String(cor)) && !colorToVariant.has(cor)) colorToVariant.set(cor, v);
          if (tamanho && !sizeToVariant.has(tamanho)) sizeToVariant.set(tamanho, v);
        }
      }

      if (has2D) {
        // Helpers 2D
  let selectedCor = null;
  let selectedTam = null;

        function getVariant(cor, tamanho) {
          const found = combos.find(c => c.cor === cor && c.tamanho === tamanho);
          return found ? found.v : null;
        }

        function renderCorList() {
          corList.innerHTML = cores.map(c => {
            const rep = combos.find(x => x.cor === c)?.v;
            const imgRel = findImageForColor(c, rep);
            const img = resolveMedia(imgRel || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''));
            return `
              <li class="ob-cor-item flex items-center gap-3 p-2 cursor-pointer hover:bg-slate-50" data-cor="${c.replace(/"/g,'&quot;')}">
                <img src="${img}" class="w-8 h-8 object-cover rounded border" alt="${c.replace(/"/g,'&quot;')}">
                <span class="text-sm text-slate-800">${c}</span>
              </li>`;
          }).join('');
        }

        function renderTamList() {
          if (!selectedCor) { tamList.innerHTML = ''; return; }
          const disponiveis = combos.filter(x => x.cor === selectedCor).map(x => x.tamanho);
          // Ordena tamanhos por uma ordem comum
          const order = ['PP','P','M','G','GG','XG','XS','S','L','XL','XXL','2XL','3XL','4XL','5XL'];
          const sorted = Array.from(new Set(disponiveis)).sort((a,b)=>{
            const ia = order.indexOf(a.toUpperCase());
            const ib = order.indexOf(b.toUpperCase());
            if (ia !== -1 && ib !== -1) return ia - ib;
            if (ia !== -1) return -1;
            if (ib !== -1) return 1;
            return a.localeCompare(b, 'pt-BR');
          });
          tamList.innerHTML = sorted.map(t => `
            <li class="ob-tam-item p-2 cursor-pointer hover:bg-slate-50" data-tam="${t.replace(/"/g,'&quot;')}">
              <span class="text-sm text-slate-800">${t}</span>
            </li>
          `).join('');
        }

        function setCor(c) {
          selectedCor = c;
          uiSelectedColor = c;
          const rep = combos.find(x => x.cor === c)?.v;
          const imgRel = findImageForColor(c, rep);
          selectedColorImageRel = imgRel || (rep && getVariantImage(rep)) || '';
          const img = resolveMedia(imgRel || (rep && rep.imagem) || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''));
          if (corLabel) corLabel.textContent = c;
          if (corThumb) { corThumb.src = img; corThumb.classList.remove('hidden'); }
          if (headerThumb) headerThumb.src = img;
          // Aplica variante com base apenas na cor (primeira combinação encontrada)
          if (rep) {
            const rep2 = { ...rep, imagem: imgRel || getVariantImage(rep) };
            applySelectedVariant(rep2);
          }
          renderTamList();
          // Define label padrão do tamanho sem alterar a variante
          const first = tamList.querySelector('li.ob-tam-item');
          if (first) {
            selectedTam = first.getAttribute('data-tam');
            uiSelectedSize = selectedTam;
            tamLabel.textContent = selectedTam;
            tamPicker.disabled = false;
          } else {
            selectedTam = null;
            uiSelectedSize = null;
            tamLabel.textContent = 'Indisponível';
            tamPicker.disabled = true;
          }
        }

        function setTam(t) {
          selectedTam = t;
          uiSelectedSize = t;
          if (tamLabel) tamLabel.textContent = t;
          tamPicker.disabled = false;
          // NÃO altera a variante; apenas atualiza rótulo de tamanho
        }

        // Wire Cor picker
        corPicker.addEventListener('click', () => {
          renderCorList();
          corDropdown.classList.toggle('hidden');
        });
        corList.addEventListener('click', (ev) => {
          const li = ev.target.closest('li.ob-cor-item');
          if (!li) return;
          corDropdown.classList.add('hidden');
          setCor(li.getAttribute('data-cor'));
        });

        // Wire Tam picker
        tamPicker.addEventListener('click', () => {
          if (tamPicker.disabled) return;
          tamDropdown.classList.toggle('hidden');
        });
        tamList.addEventListener('click', (ev) => {
          const li = ev.target.closest('li.ob-tam-item');
          if (!li) return;
          tamDropdown.classList.add('hidden');
          setTam(li.getAttribute('data-tam'));
        });

        // Fecha dropdowns ao clicar fora
        document.addEventListener('click', (e) => {
          if (!corDropdown.classList.contains('hidden') && !e.target.closest('#ob-cor-dropdown') && !e.target.closest('#ob-cor-picker')) {
            corDropdown.classList.add('hidden');
          }
          if (!tamDropdown.classList.contains('hidden') && !e.target.closest('#ob-tam-dropdown') && !e.target.closest('#ob-tam-picker')) {
            tamDropdown.classList.add('hidden');
          }
        }, { once: true });

        // Seleção inicial: NÃO pré-seleciona a primeira cor
        renderCorList();
        tamPicker.disabled = true;
        tamLabel.textContent = 'Selecione o tamanho…';
        applySelectedVariant(null);
      } else {
        // Modos parciais: color-only, size-only ou single
        let selectedCor = null;
        let selectedTam = null;

        // Mapeia cores/tamanhos mesmo quando os sets iniciais estão vazios (prioriza campo tipo)
        for (const v of variacoesOrig) {
          const titulo = v.titulo || v.info || '';
          let { cor, tamanho } = parseColorSizeFromTitle(titulo);
          const tipo = (v.tipo || '').toString().toLowerCase();
          if (tipo === 'cor' && !cor) cor = clean(titulo);
          if ((tipo === 'tamanho' || tipo === 'tam') && !tamanho) tamanho = clean(titulo);
          if (!cor) cor = deriveColorFromTitle(titulo);
          // tenta também nos objetos/arrays
          if (!cor || !tamanho) {
            const attrsObjList = [v.atributos, v.attributes, v.attr, v.opcoes, v.options, v.especificacoes];
            for (const maybe of attrsObjList) {
              if (!cor && maybe && !Array.isArray(maybe) && typeof maybe === 'object') {
                cor = cor || getFromObjKeys(maybe, ['cor', 'Cor', 'color', 'Color']);
              }
              if (!tamanho && maybe && !Array.isArray(maybe) && typeof maybe === 'object') {
                tamanho = tamanho || getFromObjKeys(maybe, ['tamanho', 'Tamanho', 'tam', 'Tam', 'size', 'Size']);
              }
            }
          }
          if (cor && !colorToVariant.has(cor)) colorToVariant.set(cor, v);
          if (tamanho && !sizeToVariant.has(tamanho)) sizeToVariant.set(tamanho, v);
        }

        let hasColorNow = hasColor || colorToVariant.size > 0;
        let hasSizeNow = hasSize || sizeToVariant.size > 0;

        function renderCorListPartial() {
          if (!hasColorNow) {
            // Cor única
            corPicker.disabled = true;
            corLabel.textContent = 'Cor única';
            return;
          }
          const items = (cores.length ? Array.from(cores) : Array.from(colorToVariant.keys()))
            .filter(c => !sizeRegex.test(String(c || '')));
          corList.innerHTML = items.map(c => {
            const rep = colorToVariant.get(c) || variacoesOrig.find(v => (parseColorSizeFromTitle(v.titulo||'').cor||'') === c);
            const imgRel = findImageForColor(c, rep);
            const img = resolveMedia(imgRel || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''));
            return `
              <li class="ob-cor-item flex items-center gap-3 p-2 cursor-pointer hover:bg-slate-50" data-cor="${c.replace(/"/g,'&quot;')}">
                <img src="${img}" class="w-8 h-8 object-cover rounded border" alt="${c.replace(/"/g,'&quot;')}">
                <span class="text-sm text-slate-800">${c}</span>
              </li>`;
          }).join('');
        }

        function renderTamListPartial() {
          if (!hasSizeNow) {
            tamPicker.disabled = true;
            tamLabel.textContent = 'Tamanho único';
            tamList.innerHTML = '';
            return;
          }
          let disponiveis;
          if (hasColorNow && selectedCor) {
            // se reconhecemos cor, filtra tamanhos dessa cor se combos existirem
            if (combos.length) {
              disponiveis = combos.filter(x => x.cor === selectedCor).map(x => x.tamanho);
            } else {
              // sem combos, lista todos tamanhos conhecidos
              disponiveis = tamanhos.length ? tamanhos : Array.from(sizeToVariant.keys());
            }
          } else {
            disponiveis = tamanhos.length ? tamanhos : Array.from(sizeToVariant.keys());
          }
          const order = ['PP','P','M','G','GG','XG','XS','S','L','XL','XXL','2XL','3XL','4XL','5XL'];
          const sorted = Array.from(new Set(disponiveis)).sort((a,b)=>{
            const ia = order.indexOf((a||'').toUpperCase());
            const ib = order.indexOf((b||'').toUpperCase());
            if (ia !== -1 && ib !== -1) return ia - ib;
            if (ia !== -1) return -1;
            if (ib !== -1) return 1;
            return String(a||'').localeCompare(String(b||''), 'pt-BR');
          });
          tamList.innerHTML = sorted.map(t => `
            <li class="ob-tam-item p-2 cursor-pointer hover:bg-slate-50" data-tam="${String(t||'').replace(/"/g,'&quot;')}">
              <span class="text-sm text-slate-800">${t || 'Único'}</span>
            </li>
          `).join('');
          tamPicker.disabled = !sorted.length;
          if (!sorted.length) tamLabel.textContent = 'Tamanho único';
        }

        function setCorPartial(c) {
          selectedCor = c;
          uiSelectedColor = c;
          const rep = colorToVariant.get(c) || variacoesOrig.find(v => (parseColorSizeFromTitle(v.titulo||'').cor||'') === c);
          const imgRel = findImageForColor(c, rep);
          selectedColorImageRel = imgRel || (rep && getVariantImage(rep)) || '';
          const img = resolveMedia(imgRel || (rep && rep.imagem) || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''));
          if (corLabel) corLabel.textContent = c;
          if (corThumb) { corThumb.src = img; corThumb.classList.remove('hidden'); }
          if (headerThumb) headerThumb.src = img;
          // Cor manda: aplica a variante da cor mesmo que existam tamanhos (tamanho é apenas rótulo)
          if (rep) {
            const rep2 = { ...rep, imagem: imgRel || getVariantImage(rep) };
            applySelectedVariant(rep2);
          }
          renderTamListPartial();
          const first = tamList.querySelector('li.ob-tam-item');
          if (hasSize && first) {
            selectedTam = first.getAttribute('data-tam');
            uiSelectedSize = selectedTam;
            tamLabel.textContent = selectedTam;
          }
        }

        function setTamPartial(t) {
          selectedTam = t;
          uiSelectedSize = t;
          if (tamLabel) tamLabel.textContent = t || 'Único';
          // Se houver cor disponível, tamanho é estático e não altera a variante
          if (!hasColorNow && hasSizeNow) {
            // Cenário somente tamanho (sem cor): aqui sim escolhe pela variação de tamanho
            let v = sizeToVariant.get(t) || variacoesOrig.find(x => (parseColorSizeFromTitle(x.titulo||'').tamanho||'') === t) || null;
            if (!v) v = variacoesOrig[0] || null;
            // Força imagem do produto principal quando não existe cor
            const prodMain = (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : getVariantImage(v));
            if (headerThumb) headerThumb.src = resolveMedia(prodMain || '');
            selectedColorImageRel = '';
            const v2 = { ...v, imagem: prodMain || getVariantImage(v) };
            applySelectedVariant(v2);
          }
        }

        // Wire Cor picker
        corPicker.addEventListener('click', () => {
          if (corPicker.disabled) return;
          renderCorListPartial();
          corDropdown.classList.toggle('hidden');
        });
        corList.addEventListener('click', (ev) => {
          const li = ev.target.closest('li.ob-cor-item');
          if (!li) return;
          corDropdown.classList.add('hidden');
          setCorPartial(li.getAttribute('data-cor'));
        });

        // Wire Tam picker
        tamPicker.addEventListener('click', () => {
          if (tamPicker.disabled) return;
          tamDropdown.classList.toggle('hidden');
        });
        tamList.addEventListener('click', (ev) => {
          const li = ev.target.closest('li.ob-tam-item');
          if (!li) return;
          tamDropdown.classList.add('hidden');
          setTamPartial(li.getAttribute('data-tam'));
        });

        // Fecha dropdowns ao clicar fora
        document.addEventListener('click', (e) => {
          if (!corDropdown.classList.contains('hidden') && !e.target.closest('#ob-cor-dropdown') && !e.target.closest('#ob-cor-picker')) {
            corDropdown.classList.add('hidden');
          }
          if (!tamDropdown.classList.contains('hidden') && !e.target.closest('#ob-tam-dropdown') && !e.target.closest('#ob-tam-picker')) {
            tamDropdown.classList.add('hidden');
          }
        }, { once: true });

        // Estado inicial
        if (!hasColorNow && !hasSizeNow) {
          // Nenhuma dimensão detectada: trata cada variação como "cor" e tamanho único
          const pseudoCores = variacoesOrig.map(v => (v.titulo || 'Opção'));
          cores.length = 0; cores.push(...pseudoCores);
          corPicker.disabled = false;
          renderCorListPartial();
          const first = corList.querySelector('li.ob-cor-item');
          if (first) {
            // usa índice para recuperar a variação
            const idx = Array.from(corList.children).indexOf(first);
            const v = variacoesOrig[idx] || variacoesOrig[0];
            corLabel.textContent = v.titulo || 'Opção';
            const img = resolveMedia(v.imagem || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : ''));
            corThumb.src = img; corThumb.classList.remove('hidden');
            tamPicker.disabled = true; tamLabel.textContent = 'Tamanho único';
            applySelectedVariant(v);
          }
        } else if (hasColorNow && !hasSizeNow) {
          // NÃO pré-seleciona cor
          renderCorListPartial();
          tamPicker.disabled = true; tamLabel.textContent = 'Tamanho único';
          applySelectedVariant(null);
        } else if (!hasColorNow && hasSizeNow) {
          // Cor única; lista tamanhos
          corPicker.disabled = true; corLabel.textContent = 'Cor única';
          renderTamListPartial();
          const firstTam = tamList.querySelector('li.ob-tam-item');
          if (firstTam) setTamPartial(firstTam.getAttribute('data-tam'));
          updateConfirmState();
        } else {
          // Teoricamente não cai aqui (coberto por has2D), mas mantém fallback
          renderCorListPartial();
          // NÃO pré-seleciona cor
          applySelectedVariant(null);
        }
        updateConfirmState();
      }

      // Botões
      modal.querySelector('#ob-cancel').onclick = () => overlay.remove();
      modal.querySelector('#ob-confirm').onclick = () => {
        // Bloqueia se a seleção for necessária e ainda não houver variante escolhida
        const needSelection = (typeof has2D !== 'undefined' && has2D) || (typeof hasColorNow !== 'undefined' && hasColorNow) || (typeof hasSizeNow !== 'undefined' && hasSizeNow);
        if (needSelection && !selectedVar) {
          showToast('Selecione a cor para continuar');
          return;
        }
        const v = selectedVar || { id: null, preco: toNumber(ob.preco), precoCmp: toNumber(ob.preco_comparacao ?? ob.precoComparacao ?? 0), titulo: '', imagem: (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '') };
        const preco = toNumber(v.preco);
        const precoCmp = toNumber(v.precoCmp);
        const tituloVar = v.titulo || '';
        const displayVar = formatCorTamanhoLabel(uiSelectedColor, uiSelectedSize, tituloVar);
        // Imagem no carrinho: se não existe cor (apenas tamanho), usa a principal do produto; senão, imagem da cor
        const noColorHasSize = (typeof hasColorNow !== 'undefined' && !hasColorNow) && (typeof hasSizeNow !== 'undefined' && hasSizeNow);
        const imgRelChosen = noColorHasSize
          ? (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '')
          : (selectedColorImageRel || (uiSelectedColor ? findImageForColor(uiSelectedColor, v) : getVariantImage(v)));
        const imagem = imgRelChosen || getVariantImage(v) || (Array.isArray(ob.fotos) && ob.fotos.length ? ob.fotos[0] : '');
  const fullSlug = ob.full_slug || ob.slug_path || ob.slug || ob.path || '';

        const variacaoId = v.id || null;
        const qtyToAdd = Math.max(1, parseInt(quantidadeDesejada, 10) || 1);
        const idxExistente = state.carrinho.findIndex((i) => {
          if (!isOrderBumpItem(i)) return false;
          const sameProduct = String(i.produtoId) === String(ob.id);
          const sameVariation = String(i.variacaoId ?? '') === String(variacaoId ?? '');
          return sameProduct && sameVariation;
        });

        if (idxExistente >= 0) {
          const atual = state.carrinho[idxExistente];
          const qtdAtual = Math.max(1, parseInt(atual.quantidade ?? atual.qty, 10) || 1);
          const novaQtd = qtdAtual + qtyToAdd;
          atual.quantidade = novaQtd;
          atual.qty = novaQtd;
          atual.preco = preco;
          atual.precoComparacao = precoCmp || preco;
          atual.preco_comparacao = precoCmp || preco;
          atual.variacao = displayVar;
          atual.variacaoId = variacaoId;
          atual.imagem = imagem;
          atual.full_slug = fullSlug;
        } else {
          const item = normalizarItemCarrinho({
            produtoId: ob.id,
            titulo: ob.titulo || 'Produto',
            preco: preco,
            precoComparacao: precoCmp || preco,
            preco_comparacao: precoCmp || preco,
            itemType: 'order_bump',
            isOrderBump: true,
            desconto: 0,
            variacao: displayVar,
            variacaoId: variacaoId,
            imagem: imagem,
            full_slug: fullSlug,
            quantidade: qtyToAdd,
          });
          state.carrinho.push(item);
        }

        persistCarrinho();
        renderCarrinho();
        refreshOrderBumpButtonsFromCart();
        setOrderBumpDesiredQty(ob.id, 1);
        updateOrderBumpCardState(ob.id);
        showToast(qtyToAdd > 1 ? `${qtyToAdd} itens adicionais adicionados` : 'Item adicional adicionado ao carrinho');
        overlay.remove();
      };
    }
  </script>
</body>

</html>
